# P1 v2.1d — 两项实现更正(设计不变)

**日期**:2026-08-28 **状态**:已实现,已测试
**范围**:设计冻结于 v2.1b/c。本文件只记录**实现层**的两处更正,
不新增假设、不改变研究问题、不改变主估计量(§7 的 `β_h` 曲线)。

---

## 1. 真实拆股集成测试的逻辑是循环的

### 错在哪

`p1/tests/test_gate0_continuity.py::test_direction_against_a_real_crsp_split`
声称用真实 CFACSHR 变动验证"调整后份额 = 原始份额 × CFACSHR"这个方向。
它取一次真实的因子变动,然后**自己造出**前后的原始份额:

```python
raw_pre, raw_post = 100.0, 100.0 / float(row["ratio"])
```

即假定"原始份额与因子成反比"。**这句假定与被检验的约定是同一句话。**
测试的原始份额一侧是从待检约定推导出来的,因此它只能证明自洽,
不能证明方向。文档字符串同时写着"hold shares constant across it",
与代码相反——两种说法都不对:真实拆股中,在管理人零交易的情况下,
**原始份额是会机械变化的**。

### 改成什么

新的 `test_direction_against_real_crsp_corporate_actions` 用 CRSP **自己报告的
SHROUT**,它与 CFACSHR 是独立的两列:

* 取所有 CFACSHR 发生变动的 permno-月(真实公司行为);
* 对两个方向各算一次**调整后份额序列在该月的相对跳跃**
  (multiply:`shrout × cfacshr`;divide:`shrout / cfacshr`);
* 要求冻结方向(multiply)的中位跳跃**显著小于**反向,且绝对量级接近零;
* 同时要求**原始**序列的中位跳跃远大于调整后的——否则这批"公司行为"
  根本不足以区分方向,测试应当跳过而不是给出虚假通过。

用中位数而非单个事件,是因为 SHROUT 也会因增发/回购在同月变动;
单个事件无法判定,一个总体可以。

配套的 `test_adjustment_cancels_a_real_action_on_a_real_holding`
在一次真实事件上端到端检验:持有恒定股份**比例**的基金,按 CRSP 观测到的
SHROUT 变动缩放头寸,调整后的 `share_turnover` 必须约为 0。

**CRSP 约定本身现已由 owner 外部核实**(份额/成交量乘以累计份额因子);
上述测试的作用是**在容器内用数据再确认一次**,而不是替代它。

### 因子必须按持仓 as-of 日期连接,不是按申报日期

N-PORT 的份额是 `genInfo/repPdDate`(Item A.2,"信息所属日期")的持仓,
申报日 (`filed`) 可以晚 30–60 天。**按 `filed` 连接 CFACSHR,会把一个 3/31 的
份额数用 5/30 的因子调整**——四月的拆股不但没被消掉,反而被**引入**,
而且事后两个数字看上去都合理。

实现:

| 位置 | 变更 |
|---|---|
| `build_nport_convexp.parse_nport` | 解析 `repPdDate` / `repPdEnd`,随 parsed 返回 |
| `build_nport_convexp.holdings_asof` | 取 `repPdDate`;**缺失则抛 `MissingAsOfDate`**,绝不回退到 `filed` |
| `main()` | 无 `repPdDate` 的基金进 `NEED_HUMAN_funds.csv`(`reason=no_repPdDate`) |
| ConvExp 行 | 新增 `holdings_asof_min` / `holdings_asof_max`(合并多基金时两者可不同) |
| `compute_continuity.factors_asof` | 按 as-of 日期取**不晚于该日**的最近因子;不前视、不默认 1.0、超 7 天缺口拒绝 |

`ops/contracts/conv_exposure_free.yaml` 中这两列与 `val_usd` 同为 **PENDING**,
必须与重建后的 parquet 同一个 commit 落地。

### 月频 CFACSHR 够不够?—— 测,不要假设

`compute_continuity.monthly_factor_sufficiency(asof_dates, daily_panel=None)`
从**真实日期**回答,而不是断言:

* 有 as-of 日期不是自然月末 → `insufficient`(月频文件根本没有该日观测);
* 全是月末但没有日频面板 → **`unknown`(NEED_HUMAN)**,不是 `sufficient`
  ——月内的因子变动在月频文件里看不见;
* 有日频面板 → 逐 (证券, 日期) 比较 as-of 日因子与月末因子,列出不一致者。

因此 `crsp.dsf` 的拉取新增 `cfacshr` / `cfacpr` / `shrout`
(`p1/wrds/tables.yaml`),并加了 `monthly_cfacshr_sufficiency` 的
NEED_HUMAN 断言:**在该函数返回 `sufficient` 之前,Gate 0 必须用日频因子。**

---

## 2. 发起人层 WCR + 首发起人样本 **不是**重复股票依赖的解

### 错在哪

计划 §15.3.0 曾用"389 只处理股票中只有 20 只跨发起人(5.1%)"
推出"发起人重抽样已覆盖 94.9% 的相关依赖",并把
"每只股票只保留首个发起人暴露"当作对剩余 5.1% 的补救。两处都不成立:

1. **分子只数了处理股票。** stacked 样本的绝大多数行是**对照**,
   而对照跨波次被大量重用(实测:2,241 只股票中 1,541 只出现在多个波次,
   单只最多 23 次)。跨发起人的同股重复主要发生在 20/389 从未计入的那一侧。
2. **分母也错了。** 相关分母是**最终回归样本**,不是处理股票数。
   在最终样本上没测过之前,任何覆盖率都是无据数字(元规则 1)。
3. **"首发起人样本"是样本修改,不是推断程序。** 丢掉跨发起人重复的股票,
   并不为**保留下来的行**提供正确的标准误。

### 改成什么

| | v2.1c | v2.1d |
|---|---|---|
| headline 推断 | 单向发起人层 WCR | **(发起人, 股票) multiway wild bootstrap**(施加原假设) |
| 单向发起人 WCR | 主 | 补充敏感性 |
| 双向 CRVE | 补充 | 补充 |
| 首发起人样本 | 主表两版之一 | **去重稳健性检验(降级)** |
| "覆盖 94.9%" | 正文论证 | **删除**,直到在最终样本上测出 |

**依赖结构保持 (发起人, 股票) 双向**——这是经济上的结构(转换决策在发起人层、
重复测量与对照重用在股票层),不因某个推断程序只支持一维而降维。
簇数少这一事实要求主程序是 wild bootstrap 而非渐近 CRVE,
但**不允许它把维度砍掉**。

~~**CITE_REQUEST**:multiway 情形下 WCB 的具体变体与少簇性质需要一条带定位符的
方法学引用……在引用与包名落定前,主程序标注 NEED_HUMAN。~~
→ **已由下方 v2.1e 附录第 1 条关闭**(owner 提供 Roodman 等 2019 与
Cameron–Gelbach–Miller 2008,实现族 Stata `boottest`)。
"不自写 bootstrap 循环"与"不得先用替代程序暂代再改口"两条**继续有效**。

### 新增前置交付物

`p1/t5_spec/measure_dependence.py` → `p1/t5_spec/dependence_profile.json`

在**最终回归样本**(处理 + 对照)上计算跨发起人 PERMNO 重用:
多发起人股票数与占比、**按行加权的重用份额**(标准误关心的是行,不是股票)、
每股票发起人数分布、处理侧与对照侧分开。列名必须显式指定,不做嗅探。

**在这份 JSON 存在之前,正文不得出现任何依赖覆盖率数字。**
`p1/tests/test_measure_dependence.py` 里有一个守卫测试:
94.9% 只能出现在解释删除的那段说明里,不能作为活的论断复活。

### 阻塞关系

信托 → 经济发起人 的人工核定 crosswalk(JPMorgan Trust I/II/Undiscovered
Managers 是同一个 JPMorgan;DFA Investment Dimensions / Dimensional Investment
Group 是同一个 Dimensional)**在主估计之前必须完成**,
因为按原始 `family` 聚类会把一个管理人拆成三个、**低估**跨发起人重用。

**它不阻塞 B1/B2,也不阻塞 Gate 0** —— 两者都不使用发起人聚类。

---

## 变更清单

```
p1/gate0_continuity/compute_continuity.py   factors_asof, monthly_factor_sufficiency, FactorJoinError
p1/tests/test_gate0_continuity.py           真实公司行为方向测试(非循环)+ as-of 连接测试
p1/t2_free/build_nport_convexp.py           repPdDate 解析、holdings_asof、per-cell as-of
p1/tests/test_build_nport_convexp.py        as-of 覆盖 + PENDING 列
p1/t5_spec/measure_dependence.py            新增(依赖结构测量)
p1/tests/test_measure_dependence.py         新增(含"94.9% 不得复活"守卫)
p1/wrds/tables.yaml                         dsf 增 cfacshr/cfacpr/shrout;两处断言改写
ops/contracts/conv_exposure_free.yaml       两列 PENDING 说明
ops/briefs/P1-T2-CONVEXP-REBUILD.md         Trap 2 扩写
docs/基金转换实验_博士研究计划.md            §6.4-4、§15.3.0、§15.3.1 改写
```

测试:487 passed, 2 skipped(两项跳过均为等待 CRSP 落地的集成测试)。
`make selfcheck`:PASSED — 86 tasks, 23 contracts。

---

# 附录 v2.1e(2026-08-28)—— A7 收窄,三处随之收紧

owner 指示:**研究设计不变**;A7 可大幅收窄。以下四项据此落定。

## 1. 推断程序的引用与实现族(A7 关闭)

| | 内容 |
|---|---|
| 方法学引用 | Roodman, MacKinnon, Nielsen & Webb (2019), *Stata Journal* 19(1):4–60 |
| 基础引用 | Cameron, Gelbach & Miller (2008), *ReStat* 90(3):414–427 |
| 实现族 | **Stata `boottest`**(支持 multiway 误差聚类 + 单独指定的 bootstrap 聚类) |
| 不作主实现 | Python `wildboottest`——其文档现声明不支持 multiway 聚类 |

v2.1d 把这一项写成 `CITE_REQUEST` + `NEED_HUMAN`;现已由 owner 提供定位符,
按元规则 1 属于可采信来源(owner 供给,非模型记忆),因此关闭。
`p1/t3_spec_preflight.md` 第 11 条同步标记为 CLOSED。

## 2. `bootcluster()`:推迟,并把**次序**写成硬约束

**已冻结**:经济误差依赖结构 = 发起人 × 股票。
**推迟**:bootstrap 权重沿哪个维度施加。可能是"multiway 发起人×股票误差聚类 +
沿小的发起人维度加权",但在最终分析样本给出下列四项之前不得写死:

1. 真实经济发起人数(过 crosswalk 之后);2. 处理发起人数;
3. 含对照样本上的跨发起人股票重用;4. 两个维度的簇不均衡度。

`measure_dependence.bootcluster_inputs` 输出全部四项,含**逆 HHI 有效簇数**
——名义簇数在一家占 93.6% 质量时几乎无意义。
~~有效簇数才是决定 bootstrap 表现的数。~~ **过强,已由 v2.1f 第 4 条降格**:
逆 HHI 只是**集中度诊断**,不是有效性判据,且不得作为选择的唯一理由。

> **次序约束**:选择必须在**观察到任何 headline 处理系数之前**书面写定并提交。
> 越过这个点,任何簇选择都是对 p 值的选择。记录:`p1/t5_spec/BOOTCLUSTER-DECISION.md`,
> 其 commit 必须早于第一次 headline 估计的 commit;`git log` 即审计轨迹。
> 守卫测试确保该记录**保持空白**,直到样本存在——事实还不存在时写下的"事前指定",
> 只是一个带日期的猜测。

## 3. crosswalk:词干匹配**不是**证据

owner 更正:token/stem 匹配**只能生成候选**,不能作为最终 crosswalk 的证据。
因此:

* 新增 `evidence_locator` 列,`load_signed()` **对任何缺该列的行拒绝**,
  包括词干匹配已经分好组的行;
* 分组状态由 `proposed_group` 改名为 `CANDIDATE_GROUP_NEEDS_FILING_EVIDENCE`;
* 门文件补上**假阳性**方向:共享系列信托(Advisors Series Trust、The RBB Fund、
  Northern Lights、Two Roads、FundVantage、Professionally Managed Portfolios)
  本就是用来容纳**互不相关**的管理人的,因此同一注册人内部逐行可能不同,
  按信托名分组是错的;~~经济发起人是发生转换的那只 series 的次级顾问~~
  **——这句已由 v2.1f 第 3 条更正**:那是把一个标签换成另一个标签。
  目标是**产生转换决策、传导共同组织冲击的经济主体**,可能是次级顾问,
  也可能是信托自身的顾问或分销平台;判定不了就写 `AMBIGUOUS`;
* 复核优先级明确写为 **Dimensional、JPMorgan、Fidelity、共享系列信托**。

## 4. `CAR^h` 的继承关系(确认)

`CAR^h` 就是 `Speed^h` 的分子,因此**异常收益基准与事件时间构造整体继承,
不新增任何自由度**,逐条写进 0-0:市场模型估计窗 [−250, −21]
(~~与 DGTW 式配对两版并报~~ —— **v2.1f 第 1 条已改判为单一主基准**;
其表述"日内市场模型为主、DGTW 仅日频及更长期限的稳健性"又被
**v2.1g 收窄**(DGTW 仅月度分辨率)与 **v2.1h 改名**
(称 **beta 调整的市场异常收益**,不称"市场模型")取代,
现行为 D-T3-11/17/22/24/25)、
`anndats`+`anntims` 事件零点、盘前/盘中/盘后三分(D-T3-12)、
midquote 取样(D-T3-13)、日历时钟(D-T3-14)、`+1d` 终点与 `+2d`/`+5d` 并报(D-T3-10)。

**唯一不继承的是分母类样本筛选**:`CAR^h` 没有分母,而按 `Speed^h` 的分母分布筛样本
会把一个**与处理状态相关**的选择规则带进主结果(转换后反应更快 ⇒ `R_final` 构成变化
⇒ 被剔除的事件与处理相关)。主表用全样本;`Speed^h` 的缩尾分位点同样不自动继承。

## 变更清单(v2.1e)

```
p1/t5_spec/BOOTCLUSTER-DECISION.md      新增(决策记录 + 次序约束)
p1/t5_spec/measure_dependence.py        bootcluster_inputs + 逆 HHI 有效簇数
p1/t5_spec/sponsor_crosswalk.py         evidence_locator 必填;候选≠证据;假阳性方向
p1/t5_spec/估计蓝图.md                   §4(ii) 实现族与引用;D-T5-8 关闭;D-T5-10 新增
p1/t3_spec/变量规格书.md                 0-0 新增"继承自 0-1 的构造"逐条表
p1/t3_spec_preflight.md                 CITE_REQUEST 11 → CLOSED
docs/基金转换实验_博士研究计划.md         §15.3.1a 引用与实现族;§15.3.1b 推迟与次序
p1/tests/test_spec_consistency.py       5 项新守卫(含"记录必须保持空白")
p1/tests/test_sponsor_crosswalk.py      证据列必填 + 假阳性方向
```

---

# 附录 v2.1f(2026-08-28)—— 基准冻结、映射目标更正、HHI 降格

owner 指示:**P1 设计不再修订**;A7 方法族与 crosswalk 架构已批准。以下四项据此落定。

## 1. CAR 基准:冻结为**唯一**主基准

原答案 D-T3-11 是"市场模型与 DGTW 两版并报"。**不合格**:两版都可能被称作
headline,等于把基准选择留到看见结果之后。现冻结:

| | |
|---|---|
| **主基准(全部 h 统一)** | **日内市场模型**:`AR_i(t_0→h) = r_i(t_0→h) − β_i · r_mkt(t_0→h)`,市场腿取**与股票腿相同的日内起止时点**,β 用日频 [−250,−21] 估 |
| 稳健性 | ~~DGTW 式配对(仅日频及更长期限)~~ → **v2.1g 收窄为"仅月度分辨率"**、β ≡ 1、市场+行业、缩放 α̂ |

**全部 h 必须用同一基准**(D-T3-18):主交付物是 β_h 的**形状**,若某个 h 换了基准,
该点的跳变分不清是信息并入的时间路径还是基准换挡。**一条基准会变的曲线不是曲线。**

## 2. 日内期限禁用 DGTW —— 已核验,不是推测

**核验结果(2026-08-28,依据本仓库自身规格)**:`变量规格书.md` 2-3 定义的 DGTW 是
**每个日历月初**做 5×5×5 特征配对,基准收益是该 cell 的**月度价值加权收益**,
数据源 `[WRDS_NEEDED: crsp.msf]`。**因此不存在任何日内时点上的 DGTW 基准值。**

用它去定义 5m/15m/30m 的异常收益,减掉的是一个在五分钟内近似恒定的量:
`AR` 与原始收益几乎相同,基准形同虚设,而表头仍写着"特征调整"。**这不会报错,
只会给出一个看起来合理的数**——所以规则必须是可执行的,不能只写在文档里。

`p1/pipeline/benchmark_policy.py`:

* `assert_benchmark_allowed("dgtw", "5m")` → raise(并解释为什么);
* `assert_curve_uses_one_benchmark({...})` → 混合基准 raise;
* `assert_is_headline("dgtw")` → 稳健性基准冒充 headline raise;
* 未命名的 `"characteristic_adjusted"` → raise("Name the exact construction")。

若日后真的构造了**在相同日内时点上**的特征组合收益,那是一个**新的稳健性提案**,
走正常流程重新登记——不是放宽这个检查。

**采购影响**:`crsp.ermport` 由"dead weight"改为**可选(仅稳健性)**——
headline 不依赖它,但 DGTW 仍是日频及脊柱二 [0,+120] 的事前指定稳健性基准。

## 3. crosswalk 的映射目标:**决策主体**,不是任何标签

v2.1e 我写成"共享系列信托的经济发起人**就是**发生转换那只 series 的次级顾问"。
**这是把一个标签换成另一个标签,同类错误。** owner 更正后的目标是:

> **映射到"合理地产生了转换决策、并会传导共同组织冲击"的经济主体。**
> 不是法律信托,**也不自动是次级顾问**。

有时决策在次级顾问;有时在信托自身的顾问;有时在**一次性转换了多只被次级顾问管理的
series 的分销平台**——后者恰恰意味着这些 series **共享**冲击,按次级顾问拆开会**高估**
独立性。问题永远是:**谁决定的,谁的冲击是共享的。**

**`AMBIGUOUS` 是允许的答案。** 治理结构确实无法判定时,填 `AMBIGUOUS`,
并在 `evidence_locator` 里写清查了什么、为什么没定论。**不得塞进启发式分组**:
被迫归组的行是"看起来已定的未知",簇数会自信地算错,而下游无从察觉。
`ambiguous_families()` 返回这些行,主估计**必须显式处理**——
headline 同时报"每个 ambiguous 自成一簇"与"并入最佳候选组"两版;
若结论在两版间移动,那本身是关于 crosswalk 承重多少的发现,**如实陈述,
不得靠挑好看的一版解决**。

## 4. 逆 HHI 降格为诊断

v2.1e 我写成"有效簇数才是决定 bootstrap 表现的数"。**过强。**
逆 HHI 只回答"多少个等大簇会有同样的集中度",别的都不回答。
bootstrap 的有效性还取决于:**被处理**的簇有几个、处理如何分布在簇之间、
个别簇对 β_h 的杠杆、以及所选变体与加权维度的性质——
一个样本可以在规模上很均衡,却仍然靠一个被处理的簇支撑。

因此:**不设阈值**,与另外三项输入及留一发起人诊断(§15.3.2)一起读,
**不得作为 `bootcluster()` 选择的唯一理由**。决策记录里新增
`treated-cluster leverage` 一行,理由栏改为"四项事实**合起来**"。

## 变更清单(v2.1f)

```
p1/pipeline/benchmark_policy.py     新增(可执行的基准策略守卫)
p1/t3_spec/变量规格书.md             0-0 (a) 重写;D-T3-11 改判;新增 D-T3-17..21
p1/t5_spec/sponsor_crosswalk.py     映射目标更正;AMBIGUOUS + ambiguous_families()
p1/t5_spec/measure_dependence.py    逆 HHI 降格为诊断(含 note 字段)
p1/t5_spec/BOOTCLUSTER-DECISION.md  同上;记录新增 treated-cluster leverage
p1/t5_spec/估计蓝图.md               §1 基准冻结段
docs/基金转换实验_博士研究计划.md     §7 分子段落写明主基准与日内禁用
p1/wrds/SHOPPING-LIST.md            ermport:dead weight → 可选(稳健性)
p1/wrds/TABLE-REQUEST.md            同上
p1/tests/test_spec_consistency.py   新增 2 项(单一主基准 + 代码级 DGTW 拒绝)
p1/tests/test_sponsor_crosswalk.py  新增 3 项(决策主体 / AMBIGUOUS / 证据仍必填)
```

测试:971 passed, 4 skipped。`make selfcheck`:PASSED。
**此后等 SEC / WRDS,不再改设计。**


---

# 附录 v2.1g(2026-08-28)—— 两项实现一致性检查

owner 指示:**不改设计**;WRDS/日内执行前只做两项一致性检查,外加一处措辞更正。

## 1. 主公式冻结,含截距处理

v2.1f 写的是 `AR = r_i − β_i · r_mkt`,**没说 α̂ 怎么办**——这正是漏洞:
`[−250,−21]` 的日频市场模型同时估出 α̂ 与 β̂,而 **α̂ 是每交易日的漂移**,
直接减到 5 分钟收益上等于多减约 78 倍(6.5 小时交易日)。

**冻结(D-T3-22)**:

$$AR^h_{i,e} = R^h_{i,e} - \hat\beta_i R^h_{m,e}$$

**不含截距,全部 h 同一条公式。** 理由与结果无关:

* **原样减 α̂** —— 量纲就错;
* **按期限缩放 α̂·(h/D)** —— 需要一个**日内漂移分配约定**(等分?按成交量?
  按波动?),本项目从未事前指定,冻结之后临时发明一个就是新增自由度;
* **不减 α** —— 唯一能让 5m 到 +1d 用同一条公式的选择,而 D-T3-18 要求如此
  (主交付物是 β_h 的**形状**)。

**代价写进论文**:`AR^h` 保留了该股在 h 内的期望漂移。
**主要依据是设计**:β_h 对每个 h 单独估计,方程含**股票固定效应 `α_i`**,
该股在该期限上**稳定的**特质漂移被 FE 吸收。次要依据:估计对象是与**带符号 SUE**
的交互系数,与 SUE 不共变的漂移不载荷到 β_h。
~~(ii) ≤1 日的漂移相对盈余公告 CAR 低若干数量级。~~
**v2.1j 删除**——未在本样本上检验过的量级论断,不得用作理由。
**稳健性并报**:`AR^h = R^h − α̂·(h/D) − β̂ R^h_m`,同样对全部 h 统一施加。

**与脊柱二的差异必须写明(D-T3-23)**:`outcomes_spine2.py` 的日频路径用
`AR_t = ret_t − α̂ − β̂·mktrf_t`,在**日频**上量纲正确,保持不变。
因此 `CAR^{+1d}` 与脊柱二 `CAR[0,+1]` **并不恒等**,相差约 α̂ × 1 日。
管道一致性 assert 必须比对**脊柱二 α 强制为 0** 的版本——否则那条 assert 必然失败,
而"修好"它最省事的办法恰好是把 α 装进脊柱零,即把量纲错误装回来。

## 2. DGTW 的可用频率:核验后收窄到月度

v2.1f 我写了"DGTW 仍是日频与 [0,+120] 的稳健性基准"。**那是对没核验过的数据下断言。**
核验结果:

* 本规格书 **2-3 提供的是月度配对 + 月度价值加权收益**,数据源 `crsp.msf`;
* 日频 DGTW 路径需要一条**日频基准组合收益**序列,**本仓库没有**;
* `crsp.ermport` 是候选,但**本容器无出网,无法核验其频率与内容**——
  元规则 1:没有定位符就不是事实。

**因此(D-T3-24)**:DGTW 稳健性收窄到**月度分辨率**;
**日内与日频均禁用**;日频 [0,+120] 路径按**市场模型**调整
(`outcomes_spine2.py` 一直就是这么实现的,**代码是对的,规格书 2-2 那一行是错的**,
已更正),**正文不得称其为"特征调整"**。

代码侧:`benchmark_policy` 把 `dgtw` 拆成 `dgtw_monthly`(仅月度期限)与
`dgtw_daily`(**允许期限集为空**,任何调用都 raise 并附三条核验要求)。
裸的 `"dgtw"` 不再是合法名字。

**采购侧**:`crsp.ermport` 从"可选(稳健性)"改为**"先问再定"**,
并把三个问题写进 `TABLE-REQUEST.md` 与 `SHOPPING-LIST.md`:
(a) WRDS 上是否存在日频 DGTW 基准组合收益序列;(b) 哪张表、什么频率;
(c) 覆盖期(2-3 记录 Wermers 分发数据到 2012,而本样本是 2021–2026)。
若 (a) 否或 (c) 覆盖不到,直接删掉这张表。

## 3. 措辞:ambiguous 对应的是**替代发起人/聚类映射**

不是"替代处理"。两次运行的**处理变量、样本、设定完全相同**,
只有定义发起人维度的**划分**不同。写成"替代处理"会让读者以为在比较两个估计对象。
`ambiguous_families()` 的文档字符串与门文件均已改写,并有守卫测试。

## 变更清单(v2.1g)

```
p1/pipeline/benchmark_policy.py     无截距主公式;dgtw 拆分为 monthly/daily;daily 空集 + 核验要求
p1/t3_spec/变量规格书.md             0-0 (a) 含截距处理;新增 D-T3-22/23/24;2-2 基准行更正
p1/t5_spec/sponsor_crosswalk.py     替代"映射"而非替代"处理"
p1/t5_spec/估计蓝图.md               §1 公式与频率段
docs/基金转换实验_博士研究计划.md     §7 分子段落同步
p1/wrds/TABLE-REQUEST.md            ermport:三个必问问题
p1/wrds/SHOPPING-LIST.md            同上;删去"日频稳健性"的断言
p1/tests/test_spec_consistency.py   新增 3 项(无截距 / +1d assert 比对零 α / DGTW 日频亦禁)
p1/tests/test_sponsor_crosswalk.py  新增 1 项(替代映射措辞)
```

**此后等 SEC / WRDS 执行,不再改设计。**

---

# 附录 v2.1h(2026-08-28)—— 市场代理整体冻结

owner 指示:**不改设计**;日内构建器运行前,冻结主 CAR 里用的**确切市场代理**。

## 1. 问题:β̂ 与事件窗市场腿必须是同一个经济代理

v2.1g 的公式写成 `AR^h = R^h − β̂_i R^h_m`,但**没说 `R_m` 是什么**,
而 0-1 沿用的 β̂ 来自 `crsp.dsi` 的 `vwretd`。于是可能出现:
**用 CRSP 价值加权指数估 β̂,再把它乘到 SPY 的日内收益上**——
同一条公式里放了两个不同的市场组合。**它不会报错**:β̂ 是合理数,
日内收益是合理数,`AR^h` 也是合理数。

事件窗腿**必须**是可交易工具:`vwretd` 与 DGTW 一样**没有日内值**。
一旦日内腿是 SPY,β̂ 的估计序列就也必须是 SPY。

## 2. 冻结:四件事一起定,不分开定

| | 冻结值 | 决策号 |
|---|---|---|
| 市场工具 | **SPY**,两条腿同一个 | D-T3-25 |
| 价格约定 | **midquote**,与股票腿一致 | D-T3-26 |
| 时段与跳空 | **仅 RTH 09:30–16:00 ET**;隔夜跳空**两条腿都不计**;`close` 取该日**实际**收盘(半日市 13:00) | D-T3-27 |
| β̂ 来源 | **SPY 自身**日频收盘对收盘收益,[−250,−21] | D-T3-28 |

**分开定就是漏洞的来源**——四项里任意一项单独看都"合理",
组合起来才可能不自洽。因此在 `benchmark_policy.MARKET_PROXY` 里作为**一个 dict**
冻结,并由 `assert_proxy_coherent()` 强制:β̂ 工具 ≠ 市场腿工具 → raise;
自洽但不是冻结工具(如 IVV)→ 也 raise(**换工具是改规格,不是运行时选项**);
市场腿用成交价 → raise;用 ETH → raise。

**盘前/盘后规则(D-T3-27)**:盘前公告 → h 自**当日首个 RTH 中点**起算;
盘后 → 自**次日首个 RTH 中点**;盘中 → 自公告时刻。
**把跳空计入股票腿而不计入市场腿(或反之),等于把一段未对冲的隔夜变动塞进
`AR^h`,同样不会报错。**

**已知局限写进论文(D-T3-28)**:β̂ 由**收盘对收盘**日频收益估出,
却被**施加到更短的 RTH 日内期限**上——这是一个**口径外推**,
**不是一个已知方向的偏误**。
**v2.1i 删除**:本处原先援引一个市场微观结构效应来断言"日内 β 被压低"。
那是在声称一个**未经本样本检验的偏误方向**,已整句删除——
连同引用的效应名一起,因为留着划掉的方向性论断仍然是方向性论断。
`β ≡ 1` 是**针对该外推的稳健性检验**,不是对偏误方向的修正。
`[NEED_HUMAN: SPY 日频收益取自 CRSP dsf 的 SPY permno,还是由同一日内数据源
聚合的收盘对收盘?两者都可,**必须二选一并记录**——不得一版一个来源。]`

## 3. 论文措辞:beta 调整的市场异常收益,不是"市场模型"

保留零截距的事件窗公式,但**不得写成 "market model"**——
没有减截距,写成市场模型会让读者以为日频 α̂ 被减到了日内窗口上。
代码里的主基准标识符也随之改名:
`intraday_market_model` → **`beta_adjusted_market`**
(以及 `beta_adjusted_market_scaled_alpha` / `beta_one_market_adjusted` /
`beta_adjusted_market_industry`),**名字就是论文里该写的东西**。

## 4. DGTW:不为保住一个稳健性标签而去建或去买

owner 明确:DGTW 在**可用序列不支持的频率上继续禁止**,
且**不得仅仅为了保留"稳健性"这个标签**去构造或采购它。
因此 `crsp.ermport` 的默认动作由"可选"改为 **"先问,默认删"**:
若不存在覆盖 2021–2026 的日频序列,DGTW 稳健性**直接放弃**,不做替代构造。
**一个为了被列出来而存在的稳健性检验,不是稳健性检验。**

## 采购侧影响

`TABLE-REQUEST.md` 新增条目 **3b:SPY 的日频收益**(不是新表,是 `crsp.dsf`
里 SPY permno 的那些行),并要求在窗口内确认 SPY 是否有可解析的 permno;
若无,β̂ 改由同一日内数据源聚合的收盘对收盘收益估,**该选择只能有一个**。
`crsp.dsi` 保留,但明确标注**不是脊柱零的市场代理**——它服务日频脊柱。

## 变更清单(v2.1h)

```
p1/pipeline/benchmark_policy.py   MARKET_PROXY / REJECTED_PROXIES / PROXY_OPEN_GAP_RULE
                                  assert_proxy_coherent();主基准改名 beta_adjusted_market
p1/t3_spec/变量规格书.md           0-0 (a) 市场代理段与论文措辞;新增 D-T3-25/26/27/28
p1/t5_spec/估计蓝图.md             §1 市场代理段
docs/基金转换实验_博士研究计划.md   §7 分子段落同步
p1/wrds/TABLE-REQUEST.md          新增 3b(SPY 日频);dsi 标注;ermport 默认删
p1/wrds/SHOPPING-LIST.md          同上;"不为标签而建/买"写进正文
p1/tests/test_spec_consistency.py 新增 4 项(代理一致 / 整体冻结 / 跳空 / 措辞)
```

测试:全仓 974+ passed。`make selfcheck`:PASSED。
**此后等 SEC / WRDS 执行。**

---

# 附录 v2.1i(2026-08-28)—— 收益口径一致性 + 跳空标注 + 措辞收窄

owner 指示:**不再改设计**;SPY 拉取前只剩一项收益定义一致性检查。

## 1. 收益口径:两条腿都用**价格收益**

事件窗的 SPY 腿是 **midquote 价格收益**——**报价中点里没有分红**。
v2.1h 冻结了工具、约定、时段与 β̂ 来源,却**没冻结收益口径**,
于是仍可能用**总收益**估 β̂ 再乘到**价格收益**上:
β̂ 里含分红成分,被应用的量不含。**`ret` 与 `retx` 都是看起来合理的日频收益,
不会报错。**

**冻结(D-T3-29)**:

> **主组合 = 价格收益股票 × 价格收益 SPY,窗口 [−250,−21];
> 估出的 β̂ 乘到同一时点的 SPY midquote 价格收益上。**

CRSP 字段:旧格式 **`RETX`**,CIZ 格式 **`DlyRetx`**;
**明确不是** `RET` / `DlyRet`。
`crsp.dsf` 的 `ret`(总收益)**仍要拉**——日频脊柱二用它;
两者并存,**各用各的,不得互换**。

代码守卫:`benchmark_policy.assert_return_concept_coherent(stock, proxy)`
——任一条腿是总收益字段 → raise;未命名的 `"daily_return"` → raise。
`tables.yaml` 的 dsf 新增 `price_return` 列与 `price_vs_total_return` 断言
(窗口内确认本账户是 `retx` 还是 `DlyRetx`)。

## 2. RTH-only 保留,但跳空必须被**标注**而不是被忘记

D-T3-27 已把隔夜跳空排除在两条腿之外。**排除不等于不存在**:
盘前公告的相当一部分反应就发生在开盘跳空里。

**冻结(D-T3-30)**:

* 盘前 / 盘后事件的 `CAR^h` **标注为"开盘后反应"(post-open response)**,
  不得与盘中事件的 `CAR^h` 混作同一个量解释;
* **隔夜/开盘跳空单独定义为一个结果变量**(上一收盘中点 → 当日首个 RTH 中点),
  单独报告,**永不并入 `CAR^h`**。并进去会同时污染两件事:
  一个**没有同期市场腿可减**的区间,和一个**与公告时段相关**的选择。

代码:`car_label(session)` 返回 `post_open_response` /
`full_announcement_response`;`assert_gap_excluded_from_car()` 对把
`overnight_open_gap` 折进 `CAR^h` 的构造 raise。

## 3. 措辞收窄:不声称日内 β 的偏误方向

v2.1h 我把局限写成"存在频率错配(某微观结构效应压低日内 β)"。
**那是在声称一个未经本样本检验的偏误方向。** 已整句删除,
连同被援引的效应名一起——留着划掉的方向性论断仍然是方向性论断。

**现行表述(D-T3-20/28)**:β̂ 由**收盘对收盘**日频收益估出,
被**施加到更短的 RTH 期限**上,这是一个**口径外推**;
`β ≡ 1` 是**针对该外推的稳健性检验**,**不是**对某个已知方向的修正。
守卫测试禁止该效应名重新出现在计划、蓝图、规格书与本备忘录中。

## 变更清单(v2.1i)

```
p1/pipeline/benchmark_policy.py   return_concept + RETURN_CONCEPTS;
                                  assert_return_concept_coherent();
                                  car_label() / assert_gap_excluded_from_car() / GAP_RULE
p1/t3_spec/变量规格书.md           0-0 (a) 收益口径与标注;新增 D-T3-29/30;D-T3-20/28 改写
p1/wrds/tables.yaml               dsf 新增 price_return + price_vs_total_return 断言
p1/wrds/TABLE-REQUEST.md          item 2 增 retx;item 3b 改为 SPY 价格收益;口径说明
p1/tests/test_wrds_layer.py       合成清单增 retx
p1/t5_spec/估计蓝图.md             §1 收益口径与跳空标注
docs/基金转换实验_博士研究计划.md   §7 同步
p1/tests/test_spec_consistency.py 新增 5 项(价格收益 / 两字段并存 / 标注 / 跳空 / 禁方向性措辞)
```

测试:983 passed, 4 skipped。`make selfcheck`:PASSED。
**停在此处,等 SEC / WRDS。**

---

# 附录 v2.1j(2026-08-28)—— 两项执行细节 + 截距理由改写

owner 指示:**不再改设计**;SPY 拉取前的两项执行细节,外加一处理由更正。

## 1. 日频 SPY 来源:定为 **CRSP**,不设代码级回退

v2.1h/i 留了一个 `NEED_HUMAN`:SPY 日频收益取自 CRSP 的 SPY permno,
还是由日内数据聚合的收盘对收盘。**现已定为 CRSP 日频文件**——
两条日频 β̂ 腿都用 CRSP 价格收益:**股票 `RETX` 对 SPY `RETX`,窗口 [−250,−21]**;
事件窗两条腿仍是**同一时点的股票/SPY midquote**。

**不得跨运行更换。** 理由是可复现性:CRSP 的 β̂ 与日内聚合的 β̂ 是**两个数**,
换一次就改变每一个 `AR^h`,而规格文字看上去完全一样。
因此 `benchmark_policy.assert_beta_source` 只接受 `crsp_daily`,
并把 `intraday_aggregate` **具名列出以便被明确拒绝**(未具名的东西没法拒绝)。
**若 SPY 在 CRSP 上没有可解析的 permno,那是 NEED_HUMAN 的规格变更**,
不是代码可以自行采取的回退。

## 2. OpenGap 结果:剔除除息日观测,并**报告剔除数**

跳空是**上一收盘 → 次日开盘的价格收益**,因此在**除息日**会机械地含进
除息下跌。留着它,次级的跳空分解会把这段下跌**读成盈余反应**——
而这种污染恰恰最不显眼:百分之几的跳空看上去与普通跳空无异。

**冻结(D-T3-31)**:剔除该股除息日的开盘跳空观测,**并把剔除数与结果同表报告**。
只剔不报会把"跳空样本掉了多少"藏起来。

**识别方式不需要分派表**:总收益含分红、价格收益不含,
因此 **`RET ≠ RETX` 当且仅当当日有分派**——两列都已在采购清单里(item 2),
不需要假设任何未确认的表名或字段名(元规则 1)。
**任一列缺失不算"无分派"**,而是**无法判定**:同样剔除,单独计数。
实现:`benchmark_policy.is_ex_distribution` / `screen_gap_sample`,
后者返回 kept / ex_distribution / undecidable 三组与三个计数。

**只影响次级的 OpenGap 结果**;`CAR^h` 从来不含跳空(D-T3-30)。

## 3. 省略截距的理由:靠**股票固定效应**,不靠量级

v2.1g 我给了两条缓解,其中第二条是"≤1 日的漂移相对盈余公告 CAR 低若干数量级"。
**那是一个未在本样本上检验过的量级断言**,不得用作理由,**已删除**。

**现行主要依据是设计**:`β_h` **对每个 h 单独估计**,方程含**股票固定效应 `α_i`**,
因此**该股在该期限上稳定的特质漂移被 FE 吸收**——被省略的截距在横截面上
就是一个股票层常数,正落在 FE 的作用域里。
次要依据保留:估计对象是与**带符号 SUE** 的交互系数,与 SUE 不共变的漂移不载荷。

**残留风险如实写明**:FE 吸收的是**稳定**漂移;若某股的漂移在事件窗内本身变化,
则不被吸收——这正是缩放 α̂ 稳健性存在的理由,不是一个可以忽略的角落。

## 变更清单(v2.1j)

```
p1/pipeline/benchmark_policy.py   beta_estimation_source + BETA_SOURCES + assert_beta_source();
                                  GAP_EXDIV_RULE / is_ex_distribution() / screen_gap_sample()
p1/t3_spec/变量规格书.md           D-T3-28 来源定案;新增 D-T3-31;D-T3-22 理由改为 FE 吸收
p1/wrds/TABLE-REQUEST.md          3b 去掉回退措辞;新增除息识别说明(无需新字段)
p1/t5_spec/估计蓝图.md             §1 来源 / 跳空除息 / FE 依据
docs/基金转换实验_博士研究计划.md   §7 同步
p1/tests/test_spec_consistency.py 新增 3 项(来源固定 / 除息剔除并计数 / FE 而非量级)
```

测试:986 passed, 4 skipped。`make selfcheck`:PASSED。
**停在此处,等 SEC / WRDS 执行。**
