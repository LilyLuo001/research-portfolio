# E2 执行手册 v1.1（完整版）：T1–T14 全部 Prompt + Agent 能力复核

替代 v1.0。变更：①补全 T4、T6–T14 全部可直接下发的 prompt（v1.0 缺失）；②新增附录 A"逐任务能力复核表"，并据复核修正两处指派（T11 撤销 DeepSeek API 联网指派——其官方 API 无原生搜索；豆包降级为仅机械核对类任务）。第 0 部分总架构、C0 上下文包、输出契约与 v1.0 相同，此处不再重复——**C0 仍须粘贴在每个 prompt 最前面**。

---

## T1｜制度事实核验（prompt 已在 v1.0，指派经复核维持）

主选 Kimi K2.6（API 内置 `$web_search`，官方文档已核实；长文档引用准确性口碑最好）；备选 Gemini 档（Google Search grounding）。**修正提示**：不要用 DeepSeek API 做本任务（无原生联网）。

---

## T2｜Dune SQL + subgraph（prompt 已在 v1.0，指派维持）

主选 DeepSeek V4，升级 Claude Sonnet 4.6。能力风险与对策见附录 A 第 2 行（任何模型都不知道 Dune 当前表结构——所以 prompt 强制"只许用我贴出的表名"）。

---

## T3｜索引管道（prompt 已在 v1.0，指派维持）

主选 Claude Code；国内替代 GLM-5.1 Coding Plan / Qwen coding agent。

---

## T4｜持有人重构、包装映射、跨链去重（v1.0 缺失，现补全）

**指派**：设计文档 → Claude Sonnet 4.6（一次短调用）；实现 → DeepSeek V4；在 agent 环境里也可整体交 Claude Code。

**Prompt T4a（设计，发 Sonnet）**：

```
【粘贴 C0】
任务(T4a): 为"RWA 代币持有人结构重构"写一份 ≤2 页的技术设计文档, 供另一名
工程师照此实现。不写代码。
需要覆盖的设计决策(逐条给出你的选择与理由):
1. 从 ERC-20 Transfer 全量日志重构地址×日余额表的算法(事件回放 vs 快照+增量),
   以及如何处理 rebasing 代币(若 registry.csv 标注 rebasing=true, 余额需按
   份额而非面值记账)。
2. 包装对(如 ACRED↔sACRED)的 1:1 守恒检验方案: 用哪些链上事件验证
   wrap/unwrap 守恒, 违反守恒时的告警与处理。
3. Wormhole 跨链 burn/mint 的配对去重方案: 配对键的定义、时间窗容差、
   无法配对时的处理(计入哪一侧、如何标记)。
4. 地址分类规则(EOA/金库合约/发行方/桥/清算人)——【硬性要求】所有分类规则
   必须设计为外部 YAML 配置, 文档中给出 YAML schema 与示例, 禁止设计成硬编码。
5. 输出表 holders.parquet 与 concentration_daily.parquet(HHI、前十占比、
   合约持有占比)的完整 schema。
6. 验收断言: Σ地址余额 = totalSupply(逐资产逐日, 容差 0)。
不确定的链上机制一律写 UNKNOWN 并列为"实现前需确认"清单, 禁止编造事件名。
```

**Prompt T4b（实现，发 DeepSeek，附 T4a 定稿）**：

```
【粘贴 C0】
任务(T4b): 严格按照附件设计文档(T4a 定稿, 我已逐条确认)实现 Python 模块
holders.py / wrap_check.py / bridge_dedup.py, 输入 flows.parquet、
Transfer 日志(路径与 schema 见附件)、registry.csv、address_rules.yaml。
规则:
- 不得偏离设计文档; 认为设计有误之处, 输出 DESIGN_ISSUE 清单而不是自行修改。
- 每个模块附 pytest, 用我提供的 fixtures(3 个资产各 200 笔样例)。
- 运行结束自动执行守恒断言并生成 conservation_report.md; 断言失败时程序
  以非零码退出, 禁止吞掉失败继续产出下游文件。
交付: 代码 + 测试 + manifest.md(含 UNKNOWN 与 DESIGN_ISSUE 清单)。
[附: T4a 定稿全文 / fixtures 说明 / Transfer 日志 schema]
```

---

## T5｜分解算法（prompt 已在 v1.0：主 prompt + 异家盲评变体，指派维持）

Claude Opus 档设计实现 + GPT-5 思考档盲评，分歧你仲裁。不省钱区。

---

## T6｜面板构建与 NAV 口径调和（v1.0 缺失，现补全）

**指派**：T6a 面板代码 → DeepSeek V4；T6b 发行方文档口径阅读 → Gemini 2.5 Flash（1M 上下文最便宜档）或 Kimi（需联网补文档时）。

**Prompt T6a（面板构建 + 15 条断言）**：

```
【粘贴 C0】
任务(T6a): 构建 panel_daily.parquet(粒度: market_id × UTC 日), 输入:
flows.parquet, decomposition.parquet, holders.parquet, oracle_updates.parquet,
nav_daily.csv(T6b 产出), rates.parquet, registry.csv。
列: gross_usd, net_usd, lambda, utilization, borrow_apy, supply_apy,
rwa_apy, spread, hhi, top10_share, contract_share, nav, nav_source,
oracle_staleness_hours, is_active。
实现 build_panel.py + assert_panel.py。assert_panel 必须逐条执行以下 15 条
断言, 全部通过才写出 parquet, 任一失败输出失败明细并以非零码退出:
A1 主键 (market_id,date) 无重复。
A2 每市场自首笔事件日至样本末无日期缺口(不活跃日也须有行, is_active=false)。
A3 gross_usd ≥ net_usd ≥ 0; lambda = gross/net 在 net>0 时成立(容差 1e-9)。
A4 utilization ∈ [0,1]; borrow_apy ∈ [0,5]; lltv 引用值与 events.parquet 一致。
A5 所有金额列单位为 USD 且 nav_source 非空。
A6 上线后市场的 nav 覆盖率 ≥95%(缺失日给出清单)。
A7 累积型资产(registry: accrual=true)的 nav 除分红日外单调不减;
   rebasing 资产的 nav 落在 [0.98,1.02] 带内, 越界给清单。
A8 oracle_updates 的时间戳 ≤ 对应观测日终点(禁止未来数据)。
A9 hhi ∈ (0,1]; top10_share ∈ (0,1]; contract_share ∈ [0,1]。
A10 gross 的日间变动与 flows 当日净流量对账, 误差 <0.5%(逐市场逐日)。
A11 spread = rwa_apy - borrow_apy 逐行可复算。
A12 lambda 的分解口径列(k 参数)与 decomposition.parquet 的配置一致。
A13 行数 = Σ各市场活跃区间天数(与 manifest 中声明一致)。
A14 无任何列全为 NaN; 每列缺失率写入 manifest。
A15 随机抽 20 行, 将各列值回溯到上游文件逐一比对(脚本自动执行并留痕)。
规则: 不得为通过断言而修改上游数据; 上游数据问题输出 UPSTREAM_ISSUE 清单。
[附: 各上游文件 schema 与样例 5 行]
```

**Prompt T6b（NAV 口径调和，发 Gemini Flash 或 Kimi）**：

```
【粘贴 C0】
任务(T6b): 我将逐个上传以下发行方的 NAV/收益率数据接口文档与法律文件节选:
Securitize(sACRED), Midas(mF-ONE, mTBILL), Superstate(USTB/USCC),
Centrifuge(JAAA/JTRSY), Circle(USYC), OpenEden(cUSDO), Pareto(FalconX CV)。
对每个资产输出一行口径登记表:
[资产, 分红机制(rebasing/累积/其他), NAV 定义(每份净值/兑换率/其他),
发布频率, 发布时间(时区), 周末与节假日是否发布, 年化收益率的计算口径,
费率扣除位置, 数据获取端点, 文档原文引用(逐字, ≤25词, 附页码/URL),
UNKNOWN 项]。
硬性规则: 每个结论必须能定位到我上传文档中的具体位置; 文档中找不到的
一律 UNKNOWN, 禁止用行业惯例填空。最后输出"口径冲突与调和建议"一节,
指出哪些资产的 NAV 不可直接横向比较、建议的统一变换。
```

---

## T7｜F1–F5 描述性事实与图表（v1.0 缺失，现补全）

**指派**：Claude Sonnet 4.6（首轮出全套脚本）；批量微调改 Qwen3.6/GLM-5。注意：任何纯 API 模型都不能自己执行绘图代码——交付物是脚本，**由你运行**；或在 Claude Code 等 agent 环境内直接跑通。

**Prompt T7**：

```
【粘贴 C0】
任务(T7): 为论文第 4 章(五个典型事实 F1–F5, 定义见附件计划节选)编写
分析与出图脚本 facts/, Python + matplotlib, 输入 panel_daily.parquet 与
decomposition.parquet。
交付:
1. compute_facts.py: 计算全部统计量, 唯一输出为 results/facts.json
   (schema: 扁平键值, 键名 = f1_gross_total_usd 之类的自述式命名)。
2. plot_f1.py … plot_f5.py: 每图一个脚本, 图中与图注的一切数字必须在
   运行时从 results/facts.json 读取, 【禁止在脚本中出现任何硬编码数值】
   (坐标轴范围除外, 且须注释理由)。
3. facts_narrative.md: 每个事实 3–5 句正文草稿, 数字一律写成
   {{facts.f1_gross_total_usd}} 形式的模板占位符, 不得写实际数值。
图规范: 单栏可读、灰度可辨、无标题内结论断言(结论进正文不进图)。
你此刻没有真实数据——先以我附上的 100 行合成样例开发, 保证脚本对真实
文件即插即用; 合成样例产生的任何数值不得出现在 narrative 中。
```

---

## T8｜H1 计量（v1.0 缺失，现补全为三段式）

**指派**：T8a 设计 → GPT-5 思考档；T8b 盲评 → Claude Opus 档（与 T8a 异家）；T8c 双实现 → DeepSeek V4（Python）+ GLM-5（R），互为复算。

**Prompt T8a（设计，发 GPT-5 思考档）**：

```
【粘贴 C0】
任务(T8a): 你是计量经济学专家。基于附件(研究计划 §5.2 全文 + panel_daily
的 schema 与描述统计), 产出 H1"循环杠杆对套息利差的敏感性"的完整计量设计
文档, 供实现与异家盲评。要求:
1. 主设定式与变量构造的逐项定义(含滞后结构选择的理由)。
2. 推断方案: curator 与资产双向聚类在有效聚类数 <10 时的失效风险评估;
   给出 wild cluster bootstrap 与随机化推断的具体实施参数, 并指明应使用
   的成熟软件包(禁止建议手搓推断程序)。
3. 货币政策冲击: 在公开可得的高频冲击序列(如 Bauer–Swanson 类)中选型,
   说明与本文日度面板的对接方式与 local projection 设定(视界、控制、
   标准误), exposure_v 的构造。
4. 威胁清单: 激励代币计划、curator 参数调整、利用率对利率的机械联动
   (IRM 内生性!)各自的偏误方向分析与对策; 特别论证 borrow_apy 由利用率
   内生决定这一点如何影响 s 的解释, 以及你的处理方案。
5. 安慰剂与稳健性设计清单(对照计划 §6 第 7–11 条逐条落实为可执行规格)。
规则: 只依据附件信息; 需要但缺失的数据特征列 NEED_INFO; 不得引用你不确定
存在的文献——文献建议单列并标注"需人工核实存在性"。
```

**Prompt T8b（盲评，发 Claude Opus 档）**：同 T8a 全文，第 5 条后追加："完成你的独立设计后，再阅读附件 B（另一位专家的设计），输出逐条比对表：一致点 / 分歧点 / 你认为对方错误之处及理由 / 建议采纳的合成方案。分歧点必须给出可判定的裁决标准，而非'两者皆可'。"

**Prompt T8c（双实现，Python 版发 DeepSeek；R 版把语言与包名替换后发 GLM-5）**：

```
【粘贴 C0】
任务(T8c-py): 按附件计量设计定稿(T8a/b 合成版, 我已确认)实现 Python 估计
脚本, 使用 linearmodels/statsmodels + 成熟 bootstrap 包(设计文档指定),
输入 panel_daily.parquet 与 shocks.csv。
输出: results/h1_py.json(每个设定一个对象: 点估计、SE 三口径、N、聚类数、
样本定义哈希)。
规则: 严格按设计文档的设定编号逐一实现, 不得增删设定; 样本过滤条件写成
配置; 任何设定无法按文档实现时输出 SPEC_ISSUE 而非变通。
说明: 同一设计文档已交由另一模型用 R(fixest+boottest 系)独立实现, 你们的
点估计将逐设定比对(容差 1e-6), 请确保样本构造完全按文档、不引入任何
未写明的默认行为(如缺失值静默删除——须显式声明)。
```

---

## T9｜压力测试引擎（v1.0 缺失，现补全为两段式）

**指派**：T9a 引擎 → Claude Code；T9b 情景参数校准（检索）→ Kimi。

**Prompt T9a（引擎，Claude Code 任务书）**：

```
【粘贴 C0】
任务(T9a): 在 repo 内实现头寸级压力测试引擎 stress/, 输入: 逐头寸快照
(positions.parquet, 由 flows 回放生成——第一步先实现回放器并与 T3 的
reconcile 结果对账), panel_daily, registry(含 T1 核实的赎回条款:
T+N、gate 条件), scenarios.yaml(T9b 产出)。
功能:
1. 情景执行器: 对 {NAV 下修 x%, 借款利率 +y bp, 赎回 T+1→T+30, 复合}
   逐头寸重算健康度, 输出触发清算头寸集合。
2. 清算可行性层: 按"清算人须白名单 + 退出仅一级申赎"的制度约束(参数化),
   在清算人容量 ∈ {0, 观测容量, ∞} 三种假设下分别求实际可清算量与
   剩余坏账, 坏账按协议规则社会化, 输出分贷方类型的损失率。
3. 传染层: 经同一金库/同一贷方的共同敞口传播二轮损失(单轮迭代即可, 记录
   收敛性)。
4. 【出厂门槛】backtest.py: 对样本内已发生的利用率触顶/挤兑日做事后复现,
   复现指标(触顶时点误差 ≤1 日、提取排队量级同数量级)不达标时, 引擎
   禁止输出任何反事实情景结果(硬编码此闸门)。
工程规则同 T3(幂等、测试、manifest)。所有制度参数只从 registry 与
scenarios.yaml 读取, 代码中不得出现任何硬编码的条款或阈值。
```

**Prompt T9b（情景校准，发 Kimi，开 `$web_search`）**：

```
【粘贴 C0】
任务(T9b): 为压力测试情景提供历史校准依据, 输出 scenarios.yaml 草案 +
校准来源表。逐参数检索一手来源(监管报告、基金年报、学术论文、指数发布方):
1. AAA 级 CLO 在 2020-03 前后 的市值/NAV 最大回撤区间;
2. 非交易型私募信贷基金(BDC/interval fund)在近两次信用压力期的 NAV
   下修幅度与赎回门槛(gate)触发案例;
3. 短久期 T-bill 基金单日 NAV 变动的历史极值量级;
4. DeFi 借贷市场在挤兑日的借款利率上冲幅度的已记录案例。
输出格式: [参数, 建议取值区间, 一手来源URL(逐条), 来源类型, 置信度,
备注]。规则同 T1: 无一手来源→UNKNOWN; 禁止凭记忆给数; 推断显式标注。
scenarios.yaml 中每个数值旁以注释标注其来源行号。
```

---

## T10｜Oracle 事件研究（v1.0 缺失，现补全）

**指派**：DeepSeek V4（模板化事件研究代码）。

**Prompt T10**：

```
【粘贴 C0】
任务(T10): 实现 H4 的两个检验, 输入 oracle_updates.parquet, flows.parquet,
panel_daily.parquet。
1. event_study.py: 以每次 NAV 喂价更新时间戳为事件时刻, 统计 [-48h,+48h]
   窗口内清算事件与"濒临清算"(健康度<1.05)头寸数的小时级分布, 输出围绕
   事件时刻的均值曲线与置信带(按市场聚类 bootstrap), 结果写
   results/h4_eventstudy.json。
2. stale_borrow.py: 比较"最近一次 NAV 更新后 <24h" vs ">48h"(周末)两组
   新增借款的 LTV 分布(KS 检验 + 分位数对比), 检验陈旧计价窗口内是否
   系统性高 LTV 借款; 结果写 results/h4_stale.json。
规则: 更新时刻的定义(区块时间戳)与时区处理必须与 panel 构建一致(引用 C0
约定); 图脚本遵循 T7 的"零硬编码数值"规范; 样本过滤写成配置。
```

---

## T11｜双周文献占坑扫描（v1.0 缺失，现补全；**指派已修正**）

**修正后指派**：主体是一个**不依赖 LLM 联网的脚本**（arXiv API + Semantic Scholar API + SSRN 站内检索链接生成），LLM 只做命中结果的相关性初筛。初筛用 Kimi（API 有官方 `$web_search`，可顺带补搜）或直接用各家免费 Chat 应用手动贴入。**撤销 v1.0 的"DeepSeek 联网档"指派**——DeepSeek 官方 API 无原生联网搜索（其联网仅存在于 Chat 应用），API 流水线里它做不了这件事。

**Prompt T11a（一次性，让 T2 同款代码模型写扫描脚本）**：

```
【粘贴 C0】
任务(T11a): 写 scan.py, 每次运行: ①调用 arXiv API 与 Semantic Scholar API,
按关键词清单(中英双语, 附后)检索最近 21 天新增论文; ②对 SSRN 生成待人工
点开的检索 URL 清单(SSRN 无稳定公开 API, 不要伪造接口——若你不确定其 API
现状, 按"生成检索URL"实现并注明); ③输出 hits_YYYYMMDD.csv
[标题, 作者, 日期, 摘要, 链接, 来源]。附 cron 说明。
关键词: RWA looping / tokenized collateral rehypothecation / embedded
leverage DeFi / tokenized treasury lending / NAV oracle liquidation /
RWA 循环 / 代币化资产 抵押 杠杆。
```

**Prompt T11b（每次扫描后的初筛，发 Kimi）**：

```
【粘贴 C0】
任务(T11b): 附件是本期文献扫描命中清单(hits.csv)与本项目研究计划摘要。
逐篇输出: [标题, 与本文重叠度(高/中/低), 判断理由(必须引用该文摘要中的
具体表述, ≤25词), 若判"高": 指出撞车的是本文哪个 Fact/假说编号]。
硬性规则: "低"也必须给引用式理由, 禁止无理由默认低; 摘要信息不足以判断时
输出 NEED_FULLTEXT 而非猜测; 不得依据标题臆测内容。
命中"高"≥1 篇时, 在表后单列"kill criterion #3 评估"一节。
```

---

## T12｜论文写作（v1.0 缺失，现补全）

**指派**：引言、贡献段、§2 概念框架 → Claude Opus 档；其余章节初稿可 GLM-5/Qwen3.6 Max 后由旗舰统稿。

**Prompt T12（逐章下发，此为通用模板）**：

```
【粘贴 C0】
任务(T12-§4): 撰写论文第 4 章(Five Facts)的英文正文, 目标期刊风格 JFQA。
输入: facts_narrative.md(含模板占位符的中文草稿), results/facts.json 的
键名清单(只有键名, 没有数值——这是有意的), 图表清单与图注, references.bib。
硬性规则(违反任一条即作废):
1. 一切实证数值只能写成 {{facts.<键名>}} 占位符, 键名必须存在于我给的
   清单中; 【禁止出现任何具体数字】, 包括"约""近半"这类由数值推出的
   定性表述——定性判断也用 {{qual.<键名>}} 占位, 由我注入。
2. 引用只允许使用 references.bib 中已有的条目 key; 你认为应补的文献,
   在文末 SUGGESTED_REFS 列出(作者+标题+你不确定的字段标 ?), 不得直接
   写入正文——正文出现 bib 之外的引用视为编造。
3. 每段开头句陈述该段主张, 主张必须能挂到某个 {{...}} 占位符或某条
   已核实制度事实(T1 表编号)上; 挂不上的主张删除。
文风: 直接、短句优先、每个 Fact 一小节、先图后释。交付 §4 全文 +
占位符使用清单 + SUGGESTED_REFS。
```

---

## T13｜红队/审稿模拟（v1.0 缺失，现补全）

**指派**：与写作者异家的旗舰（本方案：GPT-5 思考档审 Claude 写的稿），另加 DeepSeek 推理档跑一份低价平行审稿以捕互补问题。

**Prompt T13**：

```
你是 JFQA/RFS 级别的审稿人, 领域: 实证资产定价、金融中介、市场微观结构,
对 DeFi 论文的常见通病(描述性包装成因果、链上数据口径不清、样本太新)
高度敏感。附件: 论文全稿(数值已注入版) + 作者的"预期攻防表"(附件B)。
产出标准 referee report:
1. Summary(≤150词, 用你自己的话——概括失真处即是理解歧义处, 请标出)。
2. Major concerns ≥3 条: 每条必须 ①指向具体章节/表号 ②说明为何动摇结论
   ③给出可执行的修改要求(需要什么新分析/新数据)。
3. Minor comments。
4. 裁决建议(Reject / Major revision / Minor)与一句话理由。
特别指令: 附件B 是作者自认为已防守的攻击点——你的 Major 至少 2 条必须
落在附件B【未覆盖】的角度; 若你认为附件B中某条防守其实不成立, 拆掉它
也算一条 Major。禁止礼节性表扬占篇幅。
```

---

## T14｜Meta-QA 对账 agent（v1.0 缺失，现补全）

**指派**：Gemini 2.5 Flash-Lite 或豆包（机械执行清单；豆包仅限此类任务——复核确认其深度分析弱，不得上调用途）。

**Prompt T14（每次任务交付后运行）**：

```
【粘贴 C0】
任务(T14): 你是契约合规检查员, 不评价学术质量, 只按清单核对。
附件: 本次交付物(任务编号 T_x)的 manifest.md、主产出文件的 schema 摘要
与前 20 行、该任务在《连续性矩阵》中的行(附后)。
逐项输出 PASS/FAIL/UNKNOWN + 证据行号:
1. manifest 是否存在且含: 输入清单+行数/哈希、环境、局限、UNKNOWN 清单、
   下游注意事项五节。
2. 主产出列名与 C0/任务书 schema 逐列比对。
3. manifest 声明的行数与文件实际行数一致。
4. 上游 manifest 的 UNKNOWN 清单是否在本任务书中被引用(连续性检查)。
5. 连续性矩阵中本行的"交接检查点"逐项核对。
规则: 只依据附件, 不推断; 无法核对的项输出 UNKNOWN(缺什么材料)。
输出末尾: 一行结论 ACCEPT / RETURN(附退回原因编号)。
```

---

## 附录 A｜逐任务能力复核表（本次新增，回应"指定的模型是否真的能完成"）

判定标准：该任务对模型的**关键能力要求**是什么 → 指派模型是否具备（证据）→ 残余风险 → 兜底。总原则重申：**没有任何模型"知道"链上现状、Dune 表结构、合约地址、当前文献**——所有指派的可行性都建立在 prompt 强制"只用给定输入 + 只输出可验证产物"之上；能力复核复核的是"给足输入后能否胜任"。

| 任务 | 关键能力 | 指派 | 复核结论与证据 | 残余风险 → 兜底 |
|---|---|---|---|---|
| T1 | API 级联网检索 + 引用纪律 + 读长法律文件 | Kimi K2.6 | ✅ API 内置 `$web_search`（官方平台文档核实）；长文档与引用准确性是其公认强项；2M 上下文覆盖发行文件 | 检索覆盖率不及 Google 系 → 高价值问题（问题 4）用 Gemini grounding 复跑一遍求并集 |
| T2 | SQL/GraphQL 生成 | DeepSeek V4 | ✅ 代码能力对标旗舰、价格最低档（多方评测一致）；SQL 属其舒适区 | 不知道 Dune 现行 spellbook schema（**所有模型皆然**）→ prompt 已强制"只用用户贴出的表名，否则 NEED_INFO"；反复调不通升级 Sonnet |
| T3 | 多文件工程 + 自主调试执行 | Claude Code | ✅ 终端 agent 编码公开评测第一梯队（SWE-bench 最高分档） | 成本 → GLM-5.1 Coding Plan 替代（SWE-Bench Pro 公开成绩已入旗舰区间，且开源可自部署） |
| T4 | 设计（中）+ 实现（模板化） | Sonnet 设计 + DeepSeek 实现 | ✅ 两段难度都在各自能力内 | rebasing 记账是易错点 → 已列入 T6 断言 A7 与 T4 守恒断言双重兜底 |
| T5 | 深度推理 + 对抗自查 | Opus 档 + GPT-5 盲评 | ✅ 两家旗舰均为公开评测推理最强档；**此任务不存在"便宜且够用"的选项**——错误无法被执行发现，只能靠异家独立重做暴露 | 双旗舰同盲区（同类训练数据）→ ground-truth 10 笔人工标注是最终裁判，此环节不可省 |
| T6a | 数据管道 + 断言工程 | DeepSeek V4 | ✅ 模板化程度高，15 条断言已在任务书写死，不依赖模型判断 | 无 |
| T6b | 1M 级长文档抽取 + 逐字引用 | Gemini 2.5 Flash | ✅ 最便宜的 1M 上下文档（公开价目核实）；抽取式任务幻觉风险低 | "文档中间遗忘"是长上下文通病 → 分发行方分批投喂而非一次全塞；引用逐字+页码要求使漏检可人工发现 |
| T7 | 出图代码 + 排版品味 | Sonnet 4.6 | ✅ 图表代码质量与格式纪律好 | **纯 API 模型不能自己运行脚本**——已在任务书明确"交付脚本由你运行/或入 Claude Code 跑通"；合成样例开发防其臆造数据 |
| T8a/b | 计量方法论（少聚类推断、LP、IRM 内生性） | GPT-5 思考档 / Opus 盲评 | ✅ 旗舰推理档对计量文献掌握最好；已在 prompt 禁止其推荐手搓推断程序、禁止引用不确定存在的文献 | 文献名可能仍有幻觉 → SUGGESTED_REFS 机制：一切文献先隔离待人工核实，不入正文 |
| T8c | 计量代码（Py/R 双栈） | DeepSeek(Py) + GLM-5(R) | ✅ 两家均能写 fixest/linearmodels 级代码；**真正的验收不是模型能力而是双实现 1e-6 互算** | R 生态包版本差异 → 任务书要求锁 renv/requirements |
| T9a | 仿真工程 + 制度逻辑 | Claude Code | ✅ 同 T3；出厂门槛（backtest 硬闸）使"跑通但错"无法出厂 | 制度参数错 → 只从 registry/yaml 读取，源头由 T1/T9b 的 URL 级核验把守 |
| T9b | 历史金融数据检索 | Kimi | ✅ 同 T1 | 历史极值这类数字最易被模型"记忆污染" → 已强制逐参数一手 URL，无 URL 即 UNKNOWN |
| T10 | 模板化事件研究代码 | DeepSeek V4 | ✅ 舒适区 | 无 |
| T11 | 定期扫描 | 脚本 + Kimi 初筛 | ⚠️ **v1.0 指派有误已修正**：DeepSeek 官方 API 无原生联网搜索（联网仅在其 Chat 应用；API 侧需第三方搜索拼装），不能承担 API 化扫描 → 改为 arXiv/S2 API 脚本为主体（根本不需要 LLM 检索），Kimi 只做相关性初筛 | SSRN 无稳定公开 API → 脚本只生成检索 URL 供人工点开，不伪造接口 |
| T12 | 英文学术写作 | Opus 档主笔 | ✅ 学术文体最稳；两条铁律（占位符数字、bib 白名单）把写作环节的两大幻觉源（编数字、编文献）在结构上封死 | 定性夸大（"striking""dramatic"）→ 统稿时人工削 |
| T13 | 对抗性审稿 | GPT-5 思考档（异家）+ DeepSeek 推理档平行 | ✅ 异家原则成立；DeepSeek 推理档做第二份廉价报告以捕互补问题 | 审稿人模拟普遍偏"礼貌" → prompt 已强制 ≥3 Major、≥2 条落在攻防表未覆盖处、禁止表扬占篇幅 |
| T14 | 机械清单核对 | Gemini Flash-Lite / 豆包 | ✅ 任务被设计为零判断力需求；豆包"深度分析弱"的公开评价与此指派兼容（**因此豆包仅限本任务，撤销其他一切用途**） | 核对本身漏检 → 清单条目全部是可机械验证项（行数、列名、章节存在性） |

**附录 A 的两条总体结论**：
1. 经复核，v1.0 的指派有一处能力性错误（T11 的 DeepSeek API 联网）已修正，一处用途需收紧（豆包）；其余指派在"给足输入、按契约验收"的前提下均可胜任。
2. 全表最脆弱的三个环节依然是 T5、T8a、T13——它们的失败模式是"输出流畅且自信但错"，任何单一模型（无论多贵）都不构成保障，保障来自异家盲评 + ground-truth + 双实现互算这三个**结构性**机制。请勿用"换个更强模型"替代这些机制。
