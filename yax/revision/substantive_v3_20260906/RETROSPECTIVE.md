# V3 retrospective on the incomplete prior revision

Date: 2026-09-06  
Scope: code, receipts, ledgers, and delivered PDFs at parent commit `73ce268f18cc406ff751950703ddb46f54d2b0d3`  
Outcome use: this is a post-outcome process audit, not a claim about why an earlier agent reasoned as it did.

The categories below deliberately separate an observed failure from a plausible mechanism and a cause supported by code or receipts. No statement attributes an unobservable motive to a prior agent.

| Issue | Observed failure | Hypothesized cause | Verified cause or present evidence | V3 prevention |
|---|---|---|---|---|
| Static/dynamic reconciliation | The submitted appendix labels the static grouped-binomial coefficient and event-functional as different objects but does not numerically explain why their conditioning movements have opposite signs. | Module ownership and a deliverable organized around local checks rather than a cross-model identity. | `dynamics/results/STATIC_DYNAMIC_MAPPING.csv` records targets side by side, but the delivered paper contains no nesting, score-moment, fitted-stock projection, or reference-invariant post-minus-pre closure test. The internal motive is unverified. | Gate Y01--Y03 blocks interpretation until the common functional, nesting, moment, projection, and rebasing checks pass. |
| Characteristic treatment definitions | The focused characteristic module and expanded module historically used different support/group construction conventions. | Local defaults and stale treatment objects. | The current code authenticates a 468-occupation rebuilt membership and holds its `beta_quintile` map fixed for maximal/common subsets; `ANALYSIS_SPEC_AMENDMENT_2_REBUILT_TREATMENT.md` documents an earlier historical-treatment error. The submitted text still juxtaposes a 408-support focused baseline and a 455-support maximal baseline. | One serialized canonical treatment object; no local requintiling in primary matched comparisons; incompatible input hashes fail closed. |
| Household inference target | The household full-refit companion used SOC2-by-post instead of the central SOC2-by-calendar-month target. | Computational simplification or inherited pilot design. | Appendix E.3 and main Table 4 state that the 199-draw household exercise uses the less saturated family-by-post companion. It therefore cannot validate the central family-month interval. | I07 requires a same-target family-month full-refit and enough replication to assess endpoint stability. |
| Nonconvergent post-2020 and saturated seasonal fits | A coding-stable post-2020 target and highly saturated seasonality rows remained unavailable. | Separation, rank failure, ill-conditioning, solver choice, or stopping at first failure. | `FAILURE_REGISTRY.csv` records job `7468697` stopping at the first nonconvergent post-2020 fit; the delivered files do not establish which candidate cause is operative. `run_dynamics.py` records the attempted parameter burden but does not provide a second same-objective solver benchmark. | N01--N04 require boundary, rank, graph, gradient, profiling/second-solver, and same-objective diagnostics before infeasibility is declared. |
| Earnings horizon | Weekly earnings ended in March 2023 although a comparable replacement variable was identified in official documentation. | Available extract fields were treated as survey availability. | The current runner explicitly reads `EARNWEEK`; the verified DDIs for extracts 9, 11, and 12 do not contain `EARNWEEK2`. This establishes absence from current authorized inputs, not absence from IPUMS. | F07 requires official variable/version verification and a minimal authorized replacement extract or an evidence-backed input blocker. |
| Historical downstream artifacts | Some mapping, influence, service, and related narrative survived a rebuilt treatment correction. | Dependency invalidation was incomplete. | R3 contains both historical and rebuilt modules, and several scientific diagnostics in the earlier PDFs refer to frozen/historical assignments. The rebuilt characteristic amendment confirms that a prior downstream module had to be rerun after membership changed. | Canonical `spec_id`/`result_id`, dependency hashes, and an invalidation graph; no output is current merely because its file exists. |
| Simulation interpretation | The adverse simulation reported null rejection rates without defining every fitted pseudo-true target or validating the preferred wild procedures on the same DGP. | The simulation target was described structurally while inference was evaluated for model projections. | `survey_sim/INF03_INF05_ANALYSIS_SPEC.md` and the appendix report occupation-cluster normal-interval rejection under an adverse design; they do not supply the V3-required target truth for pooled, conditioned, and paired projections or compare all actual inference methods. | I02--I06 define pseudo-truths, calibrate empirical/adverse DGPs, run factor ablations, compare actual methods, and report Monte Carlo error before inferential claims. |
| Cached build versus empirical rerun | The delivered reproducibility prose could be read as stronger than the artifact available to a reviewer. | Build/audit and re-estimation were conflated in presentation. | `REPRODUCIBILITY.md` now explicitly says `run_substantive_revision_audit.sh` does not read CPS microdata or re-estimate models. Existing empirical receipts are module-specific; a fresh end-to-end V3 rerun has not yet occurred. | E10--E11 require separate commands, receipts, modes, and completion statements for restricted re-estimation and aggregate/PDF rebuild. |
| Source/report coverage | The prior R3 record reported that one earlier numbered report was unavailable. | Attachment context loss. | The V3 package now supplies that earlier report as `revision_inputs/earlier_uploaded_referee.md`, and its hash is verified. | The V3 source inventory and atomic ledger retain every source locator and separate duplicate requests from shared analyses. |
| Operational failures | Several SCC jobs failed before or after analysis for environment, path, or brittle text-check reasons. | Incomplete wrapper environment and overspecific validation. | `FAILURE_REGISTRY.csv` records a missing `scipy.stats` import, an obsolete hard-coded root, a missing HonestDiD environment variable, and a literal phrase check that failed after results were written. | New isolated worktrees, explicit environment variables, structured rather than phrase-only checks, restartable stages, and retention of all failed receipts. |

## What is not established

The inspected evidence does not establish that context loss, compute pressure, or any particular prioritization motive caused the scientific omissions. Those remain hypotheses. The verified problems are target mismatch, missing integration tests, unavailable input fields, incomplete dependency invalidation, and specific logged wrapper/environment failures.
