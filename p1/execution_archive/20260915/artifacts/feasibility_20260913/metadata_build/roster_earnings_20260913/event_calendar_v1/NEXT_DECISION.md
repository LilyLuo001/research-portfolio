# Next decision

**One concrete action:** obtain and pin authoritative documentation/source for
IBES `ANNTIMS` timezone semantics and exchange-specific regular/early-close
intervals, then authorize a source-side session adapter using that source.

The current run supports only a source-record PIT linkage diagnostic:
`103,876 → 96,573` date-valid seed-linked records.  `QTR=82,300` and
`ANN=21,576` are periodicity labels on source records, not economic-event
counts.  The 1,696 historical identifier delta is retained SCC-only and was
not queried.  No RTH/non-RTH choice, wave/event assignment, estimator or
scientific GO follows from this census.
