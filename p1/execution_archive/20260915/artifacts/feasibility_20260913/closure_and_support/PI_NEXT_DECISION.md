# P1 review closure and candidate-support decision

## Status

The finite reconciliation delta is presented for independent verification in `FINAL_DELTA_REVIEW.md`. The candidate RTH/non-RTH support is **UNKNOWN**, not zero and not positive: the permitted local exposure/calendar artifacts do not contain the verified announcement clock, earnings clocks, exchange-session mapping, horizon-feed masks, forecast-eligibility flags, clean-control calendar, or signed economic-sponsor crosswalk needed to count it.

The blank values in `SUPPORT_CENSUS.csv` and `DEPENDENCE_CENSUS.csv` are intentional `NOT_AVAILABLE` entries. They are not a claim that the candidate population is unsupported, nor a basis to choose RTH, non-RTH, a bootstrap, or a different inference method.

## What is resolved

- The proposed contract now describes a non-self-authorizing scope lint, explicit candidate standardization functional, directional high-acceleration condition, estimated-reference covariance language, and bounded synthetic-input checks.
- The raw signed high-minus-low SUE-slope DID remains distinct from timing. Pure timing, H3, terminal equivalence margins, realized estimator rank, empirical power, and inference validity remain unapproved/unmeasured.
- A connected component is not evidence that all pairwise errors covary: the documented A--B--C algebra has one graph component while `Cov(A,C)=0`. Conversely, component counts do not validate a component multiplier or any crossed/multiway alternative.

## Single next action

Authorize the **metadata-only protected census view** specified verbatim in `MINIMUM_METADATA_REQUEST.md`, subject to the stated PI contract and protected-view authorization. It must return only row-filtered support/dependence metadata plus a provenance/hash receipt, never earnings/forecast values, SUE, prices, returns, quotes, CARs, coefficients, p-values, rankings, or model-selection outputs.

That one view will determine whether the candidate RTH and non-RTH populations, complete-calendar exclusions, common six-horizon support, event reuse, and component topology can be measured. It does not authorize a treatment-effect estimate, a session choice, a data purchase, SCC work, or an inference-method choice.
