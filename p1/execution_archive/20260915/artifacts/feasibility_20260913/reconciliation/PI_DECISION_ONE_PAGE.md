# P1 contract reconciliation — decision for PI review

**Status: BLOCKED_RECONCILIATION/REVIEW.** The required independent referee found fixable fail-closed and specification defects in its sole permitted focused re-review. They have been mechanically repaired, but the bounded protocol permits no second referee review; therefore this document is a PI decision draft, not a claim that reconciliation has passed. It is not an approval to extract data, run SCC, purchase data, open sealed outcomes, estimate an effect, or change the feasibility verdict. The research remains **HOLD_DESIGN + HOLD_DATA**.

## Recommended coherent contract

Approve, if the PI accepts the identification limits, a wave-level differential package estimand: within each verified conversion package, compare top versus bottom terciles of **positive, PRE-announcement** ownership. The low tier is exposed, so the primary causal statement is a high-versus-low differential conversion-package response, not an untreated comparison, AP-arbitrage effect, or universal ownership-dose derivative.

Use the earliest independently verified public announcement instant `A_w`; measurements used for exposure must predate it, even if a later filing retrospectively reports predetermined ownership. Define implementation `I_w` from a PI-approved package rule; classify observations strictly before `A_w` as PRE, omit the transition `[A_w,I_w)`, and classify after `I_w` as POST. A complete competing-conversion calendar is required; prior 8,801/30 old-clock support does not prove eligibility under this clock.

Estimate signed standardized SUE-response slopes separately by wave and aggregate pre-frozen estimable waves equally, preserving the covariance of reused economic earnings events. The primary result is the raw signed high-minus-low slope DID curve. A normalized relative-shape DID is supplementary and requires its own normalized-shape parallel-trends restriction plus joint reference uncertainty. “Pure timing” remains blocked unless stronger tier-specific transport, directional, terminal-equivalence, and terminal-sign conditions are later established. H3 remains blocked.

Use the fixed RTH candidate rule (release at least 60 trading minutes before actual close, open-inclusive/close-exclusive, common six-horizon live-quote mask, high/low support and identified design); otherwise call the primary **NOT_ESTIMABLE**, with no sample-size fallback. The fixed SUE recipe is signed actual minus pre-release median consensus, latest analyst forecast within 90 days, at least two analysts, split-consistent pre-announcement price scaling, and a unique-event pre-first-announcement standard deviation. The proposed five-point shape and `0.05 B*` terminal margins must be treated as economic choices; `B*` and its covariance remain part of inference.

## Identifying claim and limits

Under conditional parallel changes in standardized signed SUE slopes, overlap, stable surprise measurement/selection, and no differential sponsor or conversion-spillover shocks, the raw curve identifies a differential package effect. It does not identify a group-specific causal effect, a no-conversion counterfactual, market efficiency, or fundamental value at `+1d`. Component-multiplier inference is a proposed conservative amendment, conditional on across-component independence; confirmatory inference remains blocked if its dependency graph collapses or cannot meet a pre-signed validity protocol.

## Irreducible PI decisions

1. Approve or reject the positive-tercile comparator, announcement/implementation clock, complete-calendar exclusion, RTH scope, SUE recipe, and equal-wave raw-slope estimand as one contract.
2. Approve or reject the supplementary shape language, the economic margins, and the component-dependence inference amendment; no timing/completion claim is available without the additional evidence specified in the YAML.
3. If approving a bounded next step, authorize only the purpose-scoped protected metadata/design extract described in `PROTECTED_EXTRACT_REQUEST.PROPOSED.md`; do not authorize sealed outcomes, SCC jobs, or purchases through this decision.

## Concrete next action

The PI should either sign the reconciled contract and issue a separate, purpose-scoped protected-view authorization, or return the numbered decisions above for revision. Until then, no further empirical run is permitted.
