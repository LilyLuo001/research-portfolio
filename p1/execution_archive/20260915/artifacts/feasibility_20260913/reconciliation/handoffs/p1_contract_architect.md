# P1 contract architect — proposed scientific field map

Status: PROPOSED; `pi_approved=false`, `extraction_approved=false`, `outcome_access_approved=false`; all new decisions PENDING. Requested routing: **gpt-6-astra/high**, as dispatched by coordinator; no backend telemetry independently exposed. Usage: **NOT_OBSERVED**. This handoff supplies no economic final verdict and preserves HOLD_DESIGN + HOLD_DATA.

## Recommended contract

**population / candidate_designs.** Choose high versus low ownership terciles among eligible US common stocks with positive PRE-announcement conversion ownership, within each wave. Compute empirical inverse-CDF cutpoints at 1/3 and 2/3 before earnings-coverage selection: low `D<=q1`, middle `q1<D<=q2`, high `D>q2`; never split identical doses. Empty extreme groups or coincident cutpoints make that wave ineligible. Retain middle/zero observations; middle-versus-low, continuous-positive-dose and 0.5%-support contrasts remain secondary. Report absolute high–low dose gaps, unequal group sizes, all-sponsor and ex-Dimensional results. Low exposure is treated; rank contrasts are neither no-conversion effects nor common ownership increments.

**exposure / timing.** Define `A_w` as earliest independently verified public conversion announcement among constituent funds, preserving source/timestamp uncertainty. Use its earliest possible instant for conservative exclusion. Holdings and split-consistent denominator must predate that instant; allow later-published filings only as retrospective measurement of predetermined economic ownership, explicitly distinguishing availability from as-of dates. No post-announcement transactions enter dose. Implementation `I_w` is the latest constituent legal-effective/first-ETF-trading instant. Use earnings timestamps in `[A−24 calendar months,A+24 calendar months)`: PRE strictly before A; POST at/after I; omit `[A,I)`. Entire response windows must stay within their regime and verified coverage. Unknown boundaries block eligibility. A complete conversion calendar excludes stock-wave cells exposed to another conversion before or within this window, including low doses; synchronized constituent conversions form one package. No first-wave recoding or post-holdings-continuity filtering. Report truncation and new-clock attrition; historical support does not carry over.

**causal_contract.** Let `b_gph` denote the model-standardized signed SUE response slope for group g, period p, horizon h, on the fixed PRE covariate/stock population. Estimate `δ_wh=(b_H1h−b_H0h)−(b_L1h−b_L0h)` and `θ_h=W⁻¹Σδ_wh`. Identification requires equal conversion-absent slope changes across groups on that population, overlap, stable surprise measurement/selection, correct slope standardization, and no differential exposure-proportional sponsor shock or spillover. These restrictions identify differential package effects; they do not isolate AP arbitrage or eliminate selection on gains. If surprise or coverage invariance fails, report the narrower selected-population, measured-surprise association.

**nuisance / aggregation.** Fit each wave separately with equal event-row weights, all lower-order interactions, stock×wave intercepts and baseline SUE slopes, plus industry×calendar-quarter×wave intercepts and SUE slopes; omit only exactly redundant columns. Freeze industry classification before A. Standardize both groups over a common period/calendar mix while retaining group-specific PRE stock distributions. Required rank failures are reported, never repaired by dropping controls. Freeze the estimable-wave set using authorized design inputs before responses; it defines a supported-wave target. With wave influence `a_wh`, aggregate `a_h=W⁻¹Σa_wh` on unique economic earnings IDs; covariance includes every cross-wave and cross-horizon term. Reused events retain their full common shock, without automatic inverse-reuse weighting.

**measurement / sessions.** Preserve `CAR=R_stock−beta_i R_SPY`, matching midpoint price-return clocks, no alpha subtraction, CRSP RETX beta estimation over trading days [−250,−21]. Propose an intercept in that daily regression and ≥120 usable days. Chain simple RTH-only leg returns multiplicatively across sessions; opening gaps stay separate, with bid/ask diagnostics. Horizons remain 5m/15m/30m/60m/close/+1d close. Preferred primary scope is RTH releases at least 60 trading minutes before actual close, ensuring ordered horizons. Opening is inclusive, closing exclusive; exchange calendar handles DST/half-days. Require timestamp certainty and a common six-horizon live-quote mask. Pre/aftermarket remain separate secondary post-opening objects. If RTH cannot support the frozen estimator, report primary NOT_ESTIMABLE; changing scientific session requires an amendment, never a largest-sample fallback.

**sue / calibration.** Propose signed quarterly actual EPS minus median consensus, using each analyst’s latest forecast within 90 calendar days strictly before release, ≥2 analysts, identical currency/share/accounting basis. Divide by the stock’s split-consistent closing price immediately before its earliest relevant announcement, then by one frozen SD from unique eligible events before their stocks’ earliest announcements; retain this same SUE across reused stacks, never re-standardizing by treatment/group. Exact licensed field semantics and calibration units remain PENDING_METADATA. Post-treatment SUE and coverage require explicit protected permissions. H3 remains documentary co-primary but BLOCKED pending its specified decomposition; this narrower contract cannot silently complete H3.

**timing_contrast.** Keep raw θ primary. For every group/wave separately define `F_gph=b_gph/b_gpT`, `T=+1d`, and shape contrast `κ_wh=(F_H1h−F_H0h)−(F_L1h−F_L0h)`, then equal-wave average. This is a supplementary change of estimand. Its causal interpretation additionally requires parallel counterfactual changes in normalized shapes; raw-slope parallel trends does not imply that restriction. Arbitrary positive proportional amplitude changes within each group leave κ unchanged. Estimate all references jointly; use joint confidence-set inversion/projection, preserving reference–coefficient covariance. Any required terminal confidence set including zero blocks normalized interpretation for the declared target; never trim weak-reference waves. Without identified tier-specific counterfactual levels, terminal differential equivalence cannot establish pure timing. A sufficient stronger transport restriction is `b^0_g1h=b_g0h` for every group/horizon after standardization; low exposure alone cannot identify it. A stricter relative-timing claim also requires nonnegative early κ components, their mean exceeding the shape margin, and equivalence of BOTH tier-specific terminal package effects. Claiming high-group acceleration additionally requires its own positive causal shape change. Otherwise say differential signed change, conditional relative-shape change, or mixed change; finite T is not fundamental value.

**margins.** Recommend one calibration-dependent pair: minimum early-average shape change of 5 percentage points; terminal-equivalence half-width `0.05 B*`, where `B*=10000 W⁻¹Σ_w (|b_H0T|+|b_L0T|)/2` bps per one calibrated SUE SD. Early average weights are 1/4 across 5–60m. These describe meaningful redistribution and tolerated terminal contamination; neither comes from power. PI magnitude approval and numerical bps translation remain PENDING. Estimate PRE reference uncertainty jointly; absent/weak calibration blocks translation and equivalence.

**inference.** Propose an explicit amendment retaining null-imposed Rademacher/9,999/Stata family: connect unique events sharing any signed economic sponsor/wave, stock across dates, or response-calendar date; aggregate scores within connected components and use one multiplier jointly across reused rows, horizons and references. This conservatively covers stipulated dependence but can collapse to one component. Component independence and sufficient non-dominant information are additional validity conditions, not count guarantees; otherwise inference BLOCKED. No p-value, power or interval-width selection. Romano–Wolf covers the six primary signed slopes; joint shape/directional/equivalence claims use joint confidence regions.

## Mandatory deterministic checks and provenance

A: supplied heterogeneous baselines produce raw DID `(0.08,0)` but κ=0; reject common-reference “pure timing.” B: separate influences yield 0.5 despite pooled 0.9. C: duplicated unit-variance event with weights `(0.5,0.5)` has variance 1, not 0.5. D: varying reference variance/covariance changes uncertainty; zero-containing denominators fail closed. E: proposed YAML must fail approval/extraction gates. Coordinator implements; architect ran no empirical or simulation code.

Read both supplied attachments and complete DECISION/review/YAML/NEXT_ACTION, including final resolved iterator confirmation. Targeted sources: plan 477–641; BOOTCLUSTER-DECISION 1–105; owner log 84–102. Commands: `pwd`, `git status/branch/rev-parse`, `rg`, `cat`, `sed`, `wc`, `shasum` (reads successful); no-AGENTS search and initially absent handoff-directory probe exited 1. Branch `task/p1-feasibility-adjudication-20260913`; HEAD `35361cc5a8c6360e1d48da9a2aa4fd40458b0e04`; original untracked adjudication retained. Four source hashes were reported to coordinator. No outcomes, raw inputs, SCC, secrets or legacy projects opened.
