# Codex — execute the fixed $125-ceiling P1 quotation order

The research/sample decisions are in `PILOT_DATA_ORDER.md`. Do not return a request that the PI select tickers, waves, span or schemas: they are already specified. This task executes an order rather than reopening P1 feasibility or literature review.

## Roles and effort

- **Main executor:** `gpt-5.6-terra`, reasoning **medium**. Retain an existing `gpt-5.6-sol / medium` coordinator rather than restart. Own exact manifests, API metadata, budget checks, downloads if expressly authorized, native storage and receipts.
- **Independent acquisition reviewer:** separate `gpt-5.6-sol`, reasoning **high**. Verify eight stock identities and cohort membership from the cited repository locators, date/window map, costs, source scope and delivered file consistency. Review actual new outputs and final corrected artifacts, not an empty approval template.
- No nested delegation or additional econometric/literature team. Do not repeat 30-wave exposure counts or prior mathematical audits. Actual unavailable routing/usage telemetry is `NOT_OBSERVED`. Verify account/client support; do not impersonate independent agents with headings.

## Source/data decisions already made

1. W002 = June 2021 Dimensional conversion: four stocks, four earnings releases per stock, four acquisition legs per release, with duplicate benchmark requests removed.
2. W016 = March 2023 Bridgeway/BSVO conversion. Four specified stocks, four earnings dates each.
3. The exact 8 stocks/32 events are `securities.csv` and `earnings_events.csv`; 16-release budget alternative is prespecified there. The full order's outside span is 2019-07-26 through 2023-11-07, but acquire only sliced windows.
4. Core is XNAS.ITCH `bbo-1s`, stock+SPY. Fixed 4 pre-focal windows have XNAS.ITCH `mbp-1` checks. ARCX.PILLAR one-second overlap and IWM/DFAC/BSVO are lower-priority complete bundles.
5. Nasdaq venue BBO is accepted for DEVELOPMENT ONLY. Do not wait for a full-market SIP product before this small pilot, and do not relabel its output as NBBO or use its variance to certify the national-market P1 estimator.
6. Selection is based on predecessor holdings and publicly dated releases. It is not final PRE-announcement eligibility, high/low assignment, or an untreated control list. Do not require completion of the entire protected census before downloading this named public-data set. Existing protected-source permissions still apply to any companion input.

## Owner instruction required for charged execution

This document itself spends nothing. An express owner message approving this exact bounded order enables the download phase and selected PRE-focal development inspection described in the order. That instruction supersedes the previous metadata-only vendor check for THIS finite acquisition only, not the whole P1 project. If the owner has not approved spending, quote the completed order and request one concrete approval, not a contract-reconciliation cycle.

No cash spending, paid subscription, new license acceptance, new credential retrieval, or raw-data redistribution. Read `DATABENTO_API_KEY` only through the user's configured environment. Never print, store in source, hash or copy secrets. Do not use any token from conversation history.

## Execute end to end

### 1. Local input check — short
Read the order and CSVs. Run `python build_order.py` in a working COPY only if regeneration is necessary (the delivered output checks already ran). Check event dates/ID and UTC conversions. Confirm actual data product schemas/ranges and historical raw-symbol resolution with the official SDK free metadata/symbology interfaces. Do not substitute a recent name/ETF inception performance series for a historical quotation tape.

Verify repository membership using the pinned crosswalk/E007 path and named PERMNOs. A contradiction is a targeted exception for that named security, not a reason to re-audit the full repository. Not being in the final new-clock analysis sample does not invalidate its explicitly labeled development procurement role.

### 2. Exact costs — same run, not a standalone deliverable
Quote exact disjoint requests, caching responses by canonical parameters. Never equate metadata error with zero price. Do not issue 12 arbitrary AAPL/MSFT technical probes: use THIS named order. Record dataset/schema date availability separately from a quoted cost. Missing BBO schema history is not permission to use trade bars or a different time period.

Apply the order's fixed budget algorithm: preferred full32 core + XNAS MBP-1; then ARCX checks; then extra ETFs if the total remains <= $100 and within actual unused credits. If the base exceeds the threshold, select the predetermined core16 plus validation. Preserve all 8 stocks. If it also exceeds the threshold, stop before charges with the exact shortfall. $125 is the hard total ceiling and $25 is reserve, not extra scope. Include license/tax/other required costs where applicable; do not promise credits cover unknown additional fees.

The published $70/$20/$10 figures are allocation targets, not quoted prices. Reallocate optional allowance to the base within $100. Remaining credits/expiry must be confirmed from an actual owner-supplied balance or supported account information, not inferred from the public promotion. Do not ask the model to inspect a password manager. Missing authentication requires only secure local configuration, not a new research authorization.

### 3. Acquisition — when the owner has authorized it
Run sequentially or with locked reservation accounting so in-flight requests cannot overcommit. Start with the four named pre-focal validation events and their core BBO windows. Purchase each normalized interval once; store native DBN and metadata. Use documented batch request/re-download semantics where appropriate and verify cost/receipt behavior. A failed/partial potentially charged request is reconciled before retrying. Never silently repeat paid requests to recreate CSV formatting.

Freeze the selected order/cost receipt before prices are inspected. Continue approved complete bundles; do not change names/dates after seeing movement. Missing required coverage or symbol availability is reported by request ID. Do not relabel an incomplete download as the full pilot.

### 4. Limited QA and handback
Raw files remain under the applicable license in the user's chosen storage. Derive a unique `(dataset, instrument_id, interval_end)` one-second BBO table, retaining original timestamps/flags. Do not fabricate missing quote origin times from last trade times. Keep a genuine unchanged state distinct from unknown feed coverage or withdrawals. Compare exact endpoint states with MBP-1 on the four overlap windows and record the result. No smoothness-based clock selection, nearest-minute fuzzy joins or price-dependent candidate selection.

The default response inspection is PRE-focal development only, including source-specific bid/ask and endpoint coverage. PRE-focal does not mean never-treated: label W016 earlier-conversion overlap. Post-focal prices are downloaded/archived separately and remain unavailable for substantive response curves and treatment estimates pending explicit owner analysis permission. A unioned SPY file may cover multiple events: authorize access by event/window, not merely by a filename labeled PRE.

Map issuer-date source locators to existing IBES metadata where authorized. Exact ANNTIMS semantics are not required to buy the date-wide slices; actual response calculations require an independently documented release clock. No guessing UTC/ET, using the conference-call time as earnings release, or deriving a release time from a price jump. Do not expand protected EPS/forecast permissions through this quote order. Do not run a treatment test, power simulation or literature-gap decision.

## Required final outputs

- `SELECTED_ORDER.csv` and `COST_RECEIPT.json`: exact original/selected bundle scope, gross quotes, actually consumed credits, reserve, unavailable requests and final request/file hashes.
- `DOWNLOAD_MANIFEST.csv`: request ID, dataset/schema, symbol mapping, exact bounds, job/request ID, path, bytes, records, completion status and hash. No raw prices in the conversation.
- `PILOT_COVERAGE.csv`: per event-leg stock/SPY coverage at the measured resolution, missing/withdrawn state handling and clock status. Not empirical power.
- `SCHEMA_OVERLAP_CHECK.csv`: BBO-1s versus MBP-1 on the fixed pre-focal windows; separate any venue comparison from schema comparison.
- `REVIEW.md`: independent source/cost/data reproduction and finite corrections verified on final files.
- `PILOT_DELIVERY.md`: <= 2 pages. State precisely what was acquired, amount spent, which named events are usable for the next permitted calibration, and actual exceptions. Do not return another generic “need tickers/dates” prompt.

Do not push, merge, change research contracts, open other sealed outcomes, modify source archives or buy a larger subscription. No automatic “measurement fallback paper” or research GO follows from this acquisition. The intended next scientific input is actual source-specific quote coverage/paths, not another empty metadata report.
