# P1：完成逐笔 pilot 的经济解释，裁定下一样本

2026-09-22。状态：PREPARED_NOT_EXECUTED。本文件是下一轮执行指令；本轮仅检查本地结果、代码、文献记录和远端 Git，没有访问 SCC、购数或派发代理。

## 当前事实与为何调整下一步

接受 `8c3b05261400983088b0767936b94abdf9f101c2` 的原生 tick 结果和独立复算。18 个文件、12,298,752 条 MBP-1、482,020 笔固定窗口交易；前导/尾部误入主样本的问题已修复。`PASS_WITH_LIMITATIONS` 是对应计算的复算通过，科学判断仍为 `COACTIVITY_ONLY`。

需要收紧上一轮“直接扩权重×财报面板”的建议，原因有三个。

1. 股票交易分母变化影响解释。以下是从已提交 `PAIR_COUNTS.csv` 的 EVENT / ALL_TRADES / 20µs / ALL 行读取的结果；两个窗口均为一小时，excess 是背景扣除后的 pair 数，可为非整数。

| 比较 | 普通日→财报后 excess pairs | 普通日→财报后股票成交数 | 普通日→财报后每千笔股票 excess |
| --- | ---: | ---: | ---: |
| AAPL Feb ARCX | 281.9→537.7 | 7,471→32,083 | 37.73→16.76 |
| AAPL Feb XNAS | 398.9→635.6 | 18,999→68,493 | 21.00→9.28 |
| AAPL Aug ARCX | 455.6→465.9 | 7,110→21,037 | 64.08→22.15 |
| AAPL Aug XNAS | 748.5→619.5 | 13,697→37,930 | 54.65→16.33 |

三格绝对 excess 增加，四格股票标准化率下降；以 SPY 成交数标准化，前三格也增加。因此已有数字支持“单位股票交易的关联程度下降”，尚不能统称“财报后同步活动减弱”，更不能因此否定信息机制。不同分母回答不同经济问题，不能改用最有利的一个宣称成功。

2. 原 prompt 的同一五分钟格内报价响应比较未完成。当前 `response_rows()` 对整个窗口分组，`TRADE_RESPONSE.csv` 没有五分钟格字段。原始均值与独立复算一致，但不能替代格内调整。原生方向与 midpoint fallback 的响应亦未分表。

3. 已存文献定位明确：Ernst 已研究公司信息、权重与同向同步。单独增加“权重×财报”交互不能认证贡献，也不能把观察性事件差写成因果 DiD。导师关心的是 ETF 和股票在价格发现中的作用；同步量、信息含量和谁先定价须分别回答。

## 本轮唯一任务

使用已购 18 个 DBN，完成一份有实际数字的解释性结果：

**财报后股票—SPY 同步活动的数量、占比与后续报价响应分别怎样变化？这些变化中，哪些只是交易强度和时段构成，哪些仍值得作为信息交易的候选证据？现有证据能否支撑一个有区别性贡献的下一样本？**

执行至结果、独立复算和一个明确建议完成。正常实现修复直接做；不新增 freeze、release、签字、整盘哈希或全项目审计。当前样本已见过，所有新增分析均标探索性。

## 模型与 effort

| 工作块 | 请求模型/effort | 职责 |
| --- | --- | --- |
| 主执行/研究裁定 | gpt-5.6-sol / medium | 组织任务、解释经济量、对照已存文献、完成最终决定 |
| 一名计算工程代理 | gpt-5.6-terra / medium | SCC 已购文件、五分钟聚合、指标分解、图表与可复算代码 |
| 一名独立 reviewer | gpt-5.6-sol / high | 真实结果出现后独立复算关键差额，核查分母、格内权重、方向来源和解释 |

不嵌套委派，不新增文献/架构团队，不使用 Max/Ultra。已有 Sol/medium 主线程可保留；若当前主线程为其他模型，如实记录，不假称已切换。依据实际工具能力显式请求代理模型与 effort；旧代理的 `followup_task` 若不支持覆盖，不因角色名就声称它运行所请求设置。只派发必要代理，输入仅相关文件和任务摘要，避免整段历史复制。记录 requested、dispatch settings、observable telemetry；不可见字段写 NOT_OBSERVED。此次准备没有启动任何代理。路由依据：[官方 subagent 文档](https://learn.chatgpt.com/docs/agent-configuration/subagents)，实际可用名称以当前客户端工具为准。

## 输入与运行范围

工作树：`/Users/lilyluo/research-portfolio-p1-feasibility-20260913`。

只先读：

- `p1/concentration_information/20260922_native_tick_results/RESULTS.md`、`INDEPENDENT_REVIEW.md`、`SOURCE_FIELD_MAP.md`、`REQUEST_MANIFEST.json` 和计算代码。
- `p1/concentration_information/20260921_price_discovery_literature/CONTRIBUTION_ASSESSMENT.md` 中 Ernst/Box 的对应段落。
- 本文件。

沿用 SCC 已授权数据处理范围，原始目录 `/scratch/qluo/native_tick_pilot_20260922/`，现有运行时 `/scratch/qluo/systematic_pilot_support/dbn_runtime/bin/python`。按 manifest 精确打开文件，原始输入不修改，复用可用会话。没有连接时先交付可从本地聚合表完成的数量/分母分解和已写好的 SCC 脚本，再明确一个运行缺口；不以泛化 HOLD 代替已完成结果。

本轮计算无需购数：使用已有股票/SPY、日期和双场所。SCC 保留原生逐笔、证券映射和新增细粒度中间表；Git 仅保存非可还原原始行情的统计汇总、图、代码和 receipts。旧 `ANNOUNCEMENT_PATHS.csv` 含一秒 bid/ask/mid 水平，属于抽样行情而非单纯总体统计，本轮不再复制或扩展此类导出。密钥不进入代码、命令输出或 Git。无需 WRDS、EPS、SUE 或新账号。

## 工作块一：一张完整的“数量—份额—强度”表

先用已有聚合表完成能完成的部分，再从 SCC 补齐唯一成交、方向来源和金额统计。主比较仅 B/C 的 12 个场所窗口（3 组×2会话×2场所）；A 公告窗口列为诊断，不与 RTH 混池。

每个窗口同时列：near pairs、background pairs、excess；每分钟 excess；每千笔股票和每千笔 SPY 的 excess；股票/SPY 总成交数和美元成交额；唯一已配对股票/SPY 成交数、占比、配对 multiplicity。ALL 与同向 pair 分开。背景校正后的 pair 数不是独立交易数，不把唯一配对比例也称作背景扣除结果。

主窗口不变：10:00–11:00 America/New_York；主中心 `[start,end)`；前导/尾部只服务报价端点。主 pair 定义继续为 `d=t_SPY−t_stock` 的 `[-20,+20)` 微秒，背景继续沿用已审查区间。唯一 SPY 成交与反向标记要与这个有向半开区间一致，不能交换证券后直接假定端点完全对称。所有符号分组，包括 UNKNOWN 的背景/excess 都补齐。

对每个匹配 RTH/control，报告数量差、率差和明确的分母分解。若 E 为 excess、N 为股票笔数，用恒等式：

`E_R/N_R − E_C/N_C = (E_R−E_C)/N_C + E_R×(1/N_R−1/N_C)`。

标明这是一个固定的算术分解顺序，不是因果效应；N=0 记不可计算。同步列 SPY 分母，避免只选最有利口径。三个发行人日期只有两个发行人，双场所不是独立重复样本。

## 工作块二：完成原定五分钟格内响应比较

每个 RTH/control 小时切成固定 12 个五分钟格。以中心成交确定格；post quote 可进入支持尾部。按场所、证券、窗口、五分钟格和方向来源生成配对/未配对统计。根据执行中追加的负责人指令，短时信息吸收曲线使用 50µs、200µs、1ms、10ms；同时保留 1/5/60 秒作为较慢响应参照，5 秒仍作为原规格的主要长时展示。

- 主结果只使用 MBP-1 原生 aggressor side，并严格分别展示主动买和主动卖；不得先混合无符号报价变化，以免正负抵消。midpoint fallback 只作为明确分开的敏感性结果，不得与原生方向混入主结果。任一 pair 是否同向须由两条成交的签名决定，native-only 版本需两端均为 native，不能只过滤中心成交。
- 每格计算 effective spread、signed midpoint change、realized spread 的组内均值、交易数，以及事前 quoted spread、size、signed buy/sell 构成；未知方向保持未知。
- paired 与 unpaired 同格都有有效观测才计算该格差额。单边缺组不是零；报告丢弃格、保留交易数、paired 组最小/中位/最大 n。
- 主格内差额使用有支持格的等权平均；敏感性用 paired n 加权。跨 RTH/control 差额只用两天共同支持的相同钟点格，使用同一套权重。完整小时 raw means 与标准化结果并列，不混成同一估计量。
- 报告 paired/unpaired 的事前 spread、size 和方向差异。五分钟条件化仅调整粗时段构成，未解决选择进入配对组的内生性。不要为了缩小差异反复换格、阈值或匹配规则。
- 核查报价端点没有越过已购支持边界，clear/invalid 状态不携带旧有效报价；缺端点记缺失。返回 quote age 的汇总，区分“上一条携带 BBO 的消息”和“真实 BBO 变动”，不把无更新当失效。

本轮不自动添加 Granger、IS/CS 或事后寻找领先峰。若要回答“谁主导”，先指出本设计仍缺的直接证据，不能用后续 mid 漂移代替信息生产。

## 工作块三：一次结果解释与文献裁定

给出一张三列判断表：已经观测到什么；仍有何替代解释；对导师问题能回答到哪里。

必须回答：

1. 绝对同步数量与同步份额是否相反？若相反，准确描述“同步交易增加但未赶上股票交易扩张”，不把它自动命名为挤出或增强。
2. 五分钟格内 paired/unpaired 的报价响应差距是否保留，财报后相对普通日是否一致？小样本/低支持允许给出不精确或异质结论。
3. 这些关联是否只复现 Ernst 的同步知情交易框架？本轮先使用已存定位，只有确切未决的相关命题才查原文；最多补两个一手来源，不启动新综述。
4. 即使未来“权重×财报”交互为正，它比既有论文多回答什么？权重、年份、样本量和 tick 精度本身不算贡献；观察性双差也不自带因果识别。

本轮不以百万 ticks 计算事件总体 power 或 p 值。输出各发行人/日期效果、范围、支持格和不稳定来源；交易相关、两个发行人和非随机普通日无法提供可靠全市场推断。

## 工作块四：交付一个具体后续决定，结束这次诊断

不要以“继续优化代码/再做一次审计”为研究结论。结果后只选一项：

- `METHOD_ONLY_STOP_THIS_BRANCH`：结果主要是分母/构成、常规 coactivity 或文献内复现。保留方法模块，明确停止把这三个事件扩成新颖价格发现论文；给导师一页已获知识与未识别内容。
- `SPECIFIC_EXTENSION_WORTH_TESTING`：有明确经济量级、两场所可见、条件化后仍值得检验的候选关系，并能写出相对于最近邻的区别性观测。给出唯一后续小样本设计和真实候选名单/请求草案（仅用现有获准身份与公告元数据构建）。若候选仍只是权重×盈余的复现，不能选此项。
- `MEASUREMENT_UNRESOLVED_FOR_NAMED_REASON`：只用于实际数据/实现仍不能计算核心量；交付所有可算结果和一个确切缺口。结论不显著、没有秒级新闻时间或 lack of novelty 本身不属于此类工程阻塞。

若提出新样本，明确主 estimand、为什么能更接近“ETF/股票的信息贡献”、外部消息信号/可比篮子需要什么、预期如何区分旧解释。多事件与成交强度改善精度但不自动提供新识别。权重截面异质性也不等于市场集中度随时间变化的因果效应。本轮完成 proposal/manifest 即止，不启动额外样本或采购。

## 独立复核与交付

工程计算结束后交一名 Sol/high reviewer：独立复算四格 AAPL 数量与分母分解、全部 RTH/control 五分钟加权差额；覆盖 XOM 低支持格；抽查至少20条 SCC 原始成交的签名/先前报价/端点。重点验证新增计算与最终判断，接受旧计数的已审查基础。发现实质错误直接修复并验证受影响部分，不重审整个项目。记录复核范围，不能把有限 PASS 扩大成全部研究通过。

输出目录：`p1/concentration_information/20260922_native_tick_interpretation/results/`。

交付 `RESULTS.md`（开头即数字和决定）、`ACTIVITY_DECOMPOSITION.csv`、`WITHIN_BIN_RESPONSE_SUMMARY.csv`（只导出跨格统计与支持数，细粒度格表留 SCC）、`DIRECTION_SOURCE_SUMMARY.csv`、最多三张核心图、`code/`、配置、`INDEPENDENT_REVIEW.md`、`EXECUTION_RECEIPT.json`；有确切值得测试的延伸才加 `NEXT_SAMPLE_PROPOSAL.md` 与安全请求清单。

receipt 区分实际运行、未运行、请求/实际可见模型、复用数据与购数费用（本轮0美元）。承接用户持续 Git 授权，只提交本轮安全产物和索引到现有工作分支，push 后核对远端，不合并 main。原报告留作历史；索引链接本轮解释更新。

结束时给出可向导师报告的一段结论，并准确区分测量成功、候选机制、价格发现方向及贡献。一次完成本轮闭环；没有因格式、签字或新的冻结文件而停下的步骤。
