# Authorized final closure — 2026-09-23

The user approved the preceding finite closure prompt with “按照prompt 执行”. This continues the earnings pilot under the original fixed population, dates, information blocks, horizons and test split. It does not reopen conversion, PIT fundamental-value work, other projects or a new research design.

## Assignment

- Coordinator requested: gpt-5.6-sol / medium. The existing coordinator conversation was retained; its system identifies the GPT-6 family, but its exact runtime model ID/effort are `NOT_OBSERVED`. Routine engineering and independent review are delegated.
- Existing `earnings_engineer_retry`: requested gpt-5.6-terra / medium, finite code corrections, minimum affected computations, safe tables and figures.
- Existing `earnings_independent_reviewer`: requested gpt-5.6-sol / high, independent support/weight/endpoint/loss/bootstrap review and final verification.
- The resumed-agent tool exposes status, not resolved runtime model/effort telemetry. Those settings remain `NOT_OBSERVED` beyond the recorded requests. No new agent configuration, authentication or nested delegation was used.

## Finite closure list

1. Replace cross-specification accumulation of scoring centers with per-cell support and separately deduplicated event counts.
2. Retain the old candidate-anchor path with its correct label and add a fixed candidate-minus-one-second baseline. Separate zero moves from same-direction nonzero persistence.
3. Account for absent series and invalid path endpoints, and record model sample exceptions instead of silently continuing.
4. Verify actual time-grid/window equivalence and equal issuer/event/sample weights. Missing event/control samples exposed a weighting error; missing stock series exposed a coverage denominator that incorrectly omitted absent fixed-roster members. The four affected model cells must be refit with equal available-sample weight within each event and the fixed rest-of-22 coverage denominator. Existing DBN and features are reused; these repairs implement the original specification rather than select a new model.
5. Complete independent numerical review, interpret the full fixed sensitivity set, produce the adviser-facing result and commit/push only this task's safe artifacts.

The optimized date-component bootstrap is retained and independently compared with direct resampling. There is no additional full archive scan, download, purchase, machine migration or broad feasibility review.

Raw and detailed licensed artifacts remain on SCC. Local detail copies created earlier in the task are removed only after checking their SCC counterparts; their recovery location is recorded. Only permitted summary tables, code, figures, source references and receipts enter Git. The final execution status is in `RUN_RECEIPT.json`, not inferred from this scope record.
