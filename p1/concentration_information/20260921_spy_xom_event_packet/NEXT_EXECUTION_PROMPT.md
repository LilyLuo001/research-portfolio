# P1：交付固定事件的可观测价格路径，结束输入核查循环

状态：DRAFT_FOR_PI_ADOPTION。写出或推送本文件不启动执行。PI 随后明确要求执行本 prompt，即批准下述有限测量修订及值访问；原研究的主分析 release 不因此通过。

## 目标与最终交付

在既有 research-portfolio 工作分支，承接 `85df1b1c80bce92bd11b7f4cd21ae72c8ca1e2fb` 的独立 Stage-A 复核。本轮必须交付固定 XOM 公告的 SPY/XOM 报价路径、端点和缺失情况，或用实际运行证据说明为什么连这一层都做不了。输入具备时增加明确命名的滞后持仓股票组合代理。不要把又一份来源核查报告作为唯一成果。

导师的问题仍为“ETF 与股票在哪里、何时反映公司消息”。本轮提供可观测事实与下一阶段是否值得继续的判断，不从一个公告产生全市场价格发现、因果效应、信息挤出或功效结论。

## 接受历史，限制重读

先只读本目录的 DECISION.md、EVENT_PACKET.md、INDEPENDENT_REVIEW.md、EXECUTION_RECEIPT.json，以及上一级 systematic_pilot 的 QUOTE_INPUT_MANIFEST 和必要的既有报价读取代码。缺少具体依赖才打开对应旧文件。

接受以下已复算事实，不重做全档案扫描：SPY 两个月度快照各 505 行、501 个有效证券 ID、4 行缺 ID；有效 ID 重叠/删除/新增为 500/1/1。2022-12-31 快照 eff_dt 为 2023-01-09，2023-01-31 快照 eff_dt 为 2023-02-07。它们没有证明公告时实际 SPY 持仓。XOM CUSIP 30231G10 对应的季度候选期间为 2022-12-31；41 条名义上锚点前 Detail 元数据不等于认证后的共识。

旧 H2、conversion、Refraction 和未完成的独有信息质量路线维持历史状态。

## 人员、模型与工作节奏

| 角色 | 请求模型 / effort | 工作 |
| --- | --- | --- |
| 主执行协调 | gpt-5.6-sol / medium | 维护本轮范围、写一次规格修订、整合结果和科学判断 |
| 数据与计算工程代理，最多一名 | gpt-5.6-terra / medium | 复用 SCC 文件、精确窗口补数、报价重建、图表和运行回执 |
| 独立结果复核，最多一名 | gpt-5.6-sol / high | 对具体访问代码/规格做短准入检查，结果形成后独立复算端点并检查解释 |

已有同路由代理可复用。不用 GPT-6、Max/Ultra 或嵌套委派，不另设文献/架构/监控代理。若主执行路由不匹配且客户端无法切换，记录实际可观察状态，不声称已切换。requested、dispatch accepted 与 backend telemetry 分开记录；不可见写 NOT_OBSERVED。

第一轮进展必须说明运行阶段和预期产物；实际长任务按用户已有 15 分钟偏好报告实质进展。工程进程等待期间不反复启动代理。常规语法修复后直接继续。发现影响结论的新错误必须修复并验证最终版本；不设会遗留已知错误的硬性复核次数上限，也不扩成全项目审计。

## 明确批准的测量修订

创建 SPEC_AMENDMENT.md，写清本轮新增两个层次，均不替换旧主 estimand：

1. 必做：同一固定公告窗口内，SPY 与 XOM 各自报价路径的描述性工程示范。两者现金流不同，不计算二元 IS/CS，不将领先相关或先变动自动解释为价格发现份额。
2. 条件具备时补充：以 2022-12-31 报告的证券数量构造固定的滞后持仓股票组合代理，经有据可查的公司行动调整到事件日。它不是当日实际基金持仓、NAV 或 PCF。报告经济日期早于事件不证明该版本当时已公开；可用时间未证实就明确写为回溯构造的滞后代理，不称可实时交易的信息集。

本轮以公告时间锚定路径，不计算或使用盈余意外，因此不读取 forecast、EPS 或 SUE 值，I/B/E/S 历史版本问题不再阻塞本轮描述性图表。此项降级意味着本轮不能估计新闻响应斜率、按新闻大小标准化或裁定核心公司信息覆盖命题。

## 固定对象与时钟

- 事件 ID P1-2023-08-01：XOM，2023-01-31；ETF 仅 SPY。
- 操作性 anchor：06:30 America/New_York / 11:30 UTC。沿用已核实的分发来源，保留 06:29、06:30、06:31 ET 三个预声明 anchor；不要求证明全球首发，不看价格调整时间。
- 固定请求窗口 11:15–12:45 UTC；baseline 为相应 anchor 前 5 分钟，端点为 +1、+5、+15、+30、+60 分钟。
- 明确属于盘前。按历史日期有效标识映射报价；保持 bbo-1s 的 ts_recv 语义。
- 复用现有同 feed/schema 文件，以 metadata 覆盖选定一条主要的共同 feed。存在多个候选时，事先记录固定排序；保留其他既购共同 feed 为并列诊断，不依据路径结果挑最好的一条。
- 单场所 BBO 按真实场所命名。不得把多个场所简单拼成 SIP NBBO。

## 第一段：直接跑通 SPY 与 XOM

1. 工程代理从现有 manifest 定位两只证券的历史 ID、文件、日期与窗口；SCC 上原始数据就地使用。只检查必要文件，不重哈希整个归档、不重跑 32 事件 census。
2. 在打开报价值前冻结 DIAGNOSTIC_SPEC.json：身份、时钟、feed 排序、baseline、端点、状态有效性与缺失规则、归一化公式和精确数据路径。独立 reviewer 仅检查这些内容及值读取代码。通过后生成单事件 DIAGNOSTIC_RELEASE.json，绑定最终代码、规格、来源与准入复核哈希。程序在不匹配时于值读取前退出。原 EVENT_PIPELINE_RELEASE.json 和 EMPIRICAL_RELEASE.json 仍不创建。
3. 用户执行本 prompt 授权 SCC 程序读取这一个事件所需的 SPY/XOM bid、ask、size、时间、状态及历史身份字段，并计算有限派生路径。原始报价/许可行级资料留 SCC；只导出允许的端点、粗粒度派生图和质量汇总。
4. 缺少报价时，先用已有文件确定精确缺口；在账户认证已配置且可核实的剩余额度内直接补这一窗口。后台成本和余额校验用于防止超支，无需逐单询价。没有可靠余额则不猜额度；认证缺失不得从旧聊天复制凭据。禁止充值、订阅、重复付费拉取、整日/整年扩张。记录订单与实际费用，能完成的已有文件计算照常完成。
5. 同时给出 bid/ask/mid 的 baseline=100 路径和端点相对变化，标清它们是不同证券的变化，不可比作同一现金流的贡献份额。至少包含公告前路径、固定端点、价差诊断、观察状态与三种 anchor 敏感性。
6. 复用已经核实的报价状态规则。未更新不自动判为无效；缺报价、取消、停牌、数据中断、状态不确定分别标注。若无法证明状态连续有效，则可以绘制明确标记的 observed-state 诊断，但不用于相对速度裁定。不得线性插值制造可交易端点。
7. 如果没有可比的有效共同 feed/端点，输出已得到的单边路径或缺失表，并判定 PAIR_NOT_COMPARABLE；不得改事件或换成日收益完成任务。

## 第二段：篮子只做一次限时处理，不阻塞第一段

与第一段并行使用一个不超过 30 分钟的来源/元数据处理段。这是搜索时间上限，不是报价传输耗时上限。

优先检查已有归档是否新增可用的 State Street 历史文件及已记录的官方入口。没有已授权历史文件访问就结束本轮追索，记录具体缺口；不发邮件、不接受新协议、不反复搜索、不要求 PI 提供可由程序生成的名单。取得 PCF 时仍区分 PCF 与真实持仓，不自动替换测量对象。

若只存在已知 CRSP 月度报告，按本 prompt 批准的代理定义做一次构造准入：

- 使用同一报告的股票数量，按有依据的份额单位和公司行动处理到事件日，固定证券集合；以 baseline 的股票组合市值归一化成 100。共同份额尺度会在相对路径中约去，但各股票的相对数量与公司行动仍须正确。
- 该代理是报告中股票部分的组合，不含未知基金现金/负债，不宣称 NAV 复制；未知现金不填零。四行缺 ID 的类型与未映射部分在 metadata 层分类，不能凭空当作现金或删除。
- 用报告日 percent_tna 等已许可字段披露可映射权重、未映射权重和份额单位。报告权重只能说明报告日覆盖，不能充当事件日遗漏误差上界。
- 在看响应前冻结最终代理证券集合、权重/数量和缺失政策。若映射不全，只有明确命名的“可映射报告股票子组合”可作辅助图；绝不改称完整 SPY 篮子，不用它裁定 ETF 与实际篮子的相对速度。
- 对冻结的代理集合生成精确 ID×窗口×feed 缺口表。只在同一账户额度内、能完成集合共同报价支持且身份/单位已准入时补数；不为尚未成立的篮子买数。若潜在订单超额度或共同支持不足，停止篮子补充，第一段正常交付。
- 固定集合中任何成分缺少某端点时，该端点为缺失；不按端点剔除成分并重新归一化，不对缺失证券填零收益，不用盘前旧收盘冒充当前报价。
- 代理进入值访问前，由同一 reviewer 验证新增定义及精确权限，并扩充同一个 diagnostic release；无需再次审查整个项目。

如果这段不能完成身份、单位或公司行动的准入，就记录 PROXY_NOT_READY，结束本轮篮子工作。不得因此丢弃已经完成的 SPY/XOM 路径。

## 独立复算与结果解释

有实际产物后，同一 reviewer 独立复算两个证券所有三种 anchor、六个端点的可用状态与变化值；若有代理，复算数量、baseline 及端点聚合。比较容差在复算前按源精度规定，差异逐项解决。说明是在 SCC 从原始文件重建还是只对导出表重算，不夸大复核层级。

结果只能回答：两条路径是否可观测、变化与价差/时钟不确定性是否可区分、代理是否可实际计算，以及这一测量是否值得进入多事件阶段。终端价格不等于基本价值；零更新、幅度小、路径不同均不能直接证明信息没有被吸收。

不计算单事件信息份额、领先秒数的显著性、群体响应斜率、Top/Rest 效果、p 值或经验 power。不得按这一事件的响应大小选择后续样本，也不自动把同窗口新闻归因于 XOM 财报。所有被打开的事件记录为 development/exploratory。

## 结束条件与唯一下一行动

本轮必须结束于以下实际状态之一：

- PATHS_AND_PROXY_COMPUTED：交付两证券及明确代理图表，并说明实际篮子和惊喜信号仍未通过的部分。
- PATHS_COMPUTED_PROXY_UNAVAILABLE：交付两证券图表及一个准确的篮子缺口；这是允许且有用的本轮结果。
- PAIR_NOT_COMPARABLE：交付实际文件/状态诊断、已运行部分和首个决定性失败原因。

这些状态均不等于主研究 GO。协调器基于已执行证据做一页判断：哪些测量问题已经解决，哪些还阻止原科学命题，本轮发现是否足以支持一个有限的多事件测量试验。只给一个具体下一行动；若原对象的关键源无法获得，应直接建议终止该实现或提出范围明确的后续修订，不自动再次循环索要历史文件。

不自动扩展到其他公告、年份或主研究回归。跨事件规格若值得做，附一页可审核草案即可。

## 最少交付与 Git

新目录：p1/concentration_information/20260921_xom_spy_observed_paths/。

- DECISION.md：实际状态、最重要的可观测事实、判断限制及唯一下一行动。
- SPEC_AMENDMENT.md 与 DIAGNOSTIC_SPEC.json：本轮定义与用户执行本 prompt 的批准依据。
- SOURCE_AND_GAP_MANIFEST.json、必要订单/费用 receipt、code/。
- DIAGNOSTIC_RELEASE.json：仅在具体诊断准入通过时创建。
- ENDPOINTS.csv、QUALITY_SUMMARY.csv、PATHS.png（实际可运行部分；没有图就写具体失败，不生成空图充数）。
- BASKET_PROXY_STATUS.md：实际构造结果或一次处理段后停止的准确理由。
- INDEPENDENT_REVIEW.md、EXECUTION_RECEIPT.json：真实命令、最终版本、路由、费用与复算范围。

沿用既有 Git 授权，提交本轮安全代码、图表、允许的汇总与文档到现有工作分支，验证远端；不推原始报价/许可逐行数据/凭据，不合并 main。历史文件保留，只更新接手索引。

执行时继续处理已授权的常规修复，不每完成一步就询问是否继续。若需要超出本 prompt 的科学修改或外部操作，先交付全部已获准结果，再说明具体扩展需求。
