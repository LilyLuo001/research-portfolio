# Independent review — bounded same-industry replication

Reviewer: separate `empirical_referee`, resumed from the explicitly dispatched `gpt-6-astra / high` reviewer. Interface routing accepted; backend telemetry `NOT_OBSERVED`. No nested delegation, purchases, credentials, new years, or whole-project audit.

**Final computation/publication status: PASS for the qualified, reproducible aggregate package. Scientific action: `INCONCLUSIVE_STOP_SPENDING`.** This distinction is essential: neither the support gate nor successful computation establishes power, causal identification or a zero effect.

## Pre-outcome status

**Metadata support and corrected implementation independently verified.** Final exact-file binding in `PREOUTCOME_REVIEW.json` governs whether responses may be opened. A pre-outcome pass permits only the qualified, frozen descriptive replication; it is not a research, power or causal-identification pass.

Only permitted metadata and baseline exposure covariates were read in this review. No new H2 return, delisting-return, price, quote or earnings-value field was projected. Licensed rows remain on SCC.

## Independent metadata reproduction

Separate SCC code—not the submitted support helper—recomputed the within-event five-column design, event-balanced weights, block deletions, calendar sets, issuer-receiver membership, baseline SIC and conservative own-news exclusions. Own-news checks independently joined date-valid I/B/E/S links and historical CRSP identities, projecting only the permitted identity/date/measure metadata.

| Check | Independent result |
| --- | ---: |
| Unique issuer-event-receiver rows | 4,904 |
| Issuer-events / calendar-support blocks | 12 / 9 |
| Unique receivers | 490 |
| Same-industry rows / receivers | 433 / 137 |
| Other-industry rows | 4,471 |
| Main within-event rank / columns | 5 / 5 |
| Minimum leave-one-block rank | 5 |
| Blocks with exposure variation in each industry group | 9 / 9 |
| Maximum event-balanced block-weight share | 1/6 |
| Receiver-control assignments / distinct windows | 12,999 / 24 |
| Minimum controls per receiver-event | 2 |
| Reused receiver/control-window keys | 3,163 |
| Reused receiver/control-window keys across distinct blocks | 0 |
| Cross-block event/control calendar intersections | 0 |
| Controls touching any recorded top-eight event window | 0 |
| Retained event dates touching the exposed July 28 window | 0 |
| Duplicate design keys / nonadjacent two-session windows | 0 / 0 |
| Baseline SIC disagreements / ambiguous SIC in retained rows | 0 / 0 |
| Unmatched PURE_D pairs / changed pair values | 0 / 0 |
| Own-news event/control exclusion violations | 0 / 0 |
| Receivers lacking any mapped 2023 own-EPS metadata | 0 |
| Events with positive cross-group log-exposure range overlap | 12 / 12 |
| Minimum same/other-industry rows per event | 20 / 301 |
| Same/other rows within each event's common group range | 428 / 4,035 |

Having at least one mapped own-EPS record does not prove exhaustive news coverage. The zero violation counts refer to the permitted recorded-news universe and its explicit conservative, preceding-trading-date-inclusive predicate.

The 3,163 reused keys arise within shared event-date blocks, not across the independent-calendar allocation. Thus they do not recreate the prior stage's cross-block control reuse. Calendar separation nevertheless does not prove independent shocks across repeated firms, issuers or sectors.

Independent private-file SHA-256:

- `receiver_event_design.parquet`: `c02a825c6946994e44c459a1c14af1335d569b8621eb7411c13ad1e1181749dc`
- `control_assignment.parquet`: `242ef8f836065b9161f6cad3e5b21c74ac66a09d321e2410e6460e79ad0556b5`

The superseded 11,736-row mixed-variant product is invalid and is not a gate input. The original metadata rank/weight labels do not implement the final event-FE/event-balanced model; the authoritative support gate and independent recount above govern this review.

## Scientific and exposure boundaries

The operational 8-block / 6-group-block / 25%-weight checks are reasonable predeclared triage for this narrow descriptive exercise, not mathematical guarantees of interval coverage or power. Both direct primary quantities, the fixed H1 connection scale, event-balanced H2 weighting, multiplicity adjustment and directional decision rule must remain frozen before opening responses. A direct interaction is necessary; comparing subgroup significance is not a test of their difference.

The corrected CR1 covariance convention counts five slopes plus one absorbed intercept per issuer-event. A separate synthetic noisy-data calculation using the full event-dummy design, directly formed cluster scores and sandwich covariance matched the submitted within-FE covariance to `4.80e-15`; the combined same-industry slope standard error matched to `4.10e-15`. This also verifies inclusion of the covariance between the base slope and interaction, rather than adding standard errors.

The proposed conditional cross-calendar-block independence assumption is explicit but not established by nonoverlap. Any eventual intervals and action are conditional, exploratory evidence—not ETF mediation, concentration-driven change, or validated price discovery.

The known 452 H2 delisting records process-loaded in the preceding concentration stage remain in the ledger. This metadata audit establishes neither globally pristine H2 inputs nor the absence of every historical P1 analysis. The permitted claim is a newly estimated sample relative to the named stage and recorded exposure ledger; unknown historical exposure must not be silently reset.

## Finite corrections verified before opening

1. The finalized opening guard binds the response builder, statistics/estimation code, specification/configuration and six explicit metadata/provenance inputs. It rejects changed hashes, absent files and basename collisions. The builder verifies that its actual design/control paths belong to that bound set before any response read.
2. The estimator reassesses the frozen operational support after response missingness, without refilling controls or switching estimands. A failed support check returns no certified interval; an empty response panel preserves its schema and yields zero-support diagnostics.
3. The covariance factor counts absorbed event intercepts; the independent full-dummy synthetic comparison above passes.
4. Return projection is restricted to the exact frozen event/control response dates and frozen receiver companies. Delisting-return projection is restricted to those dates and selected share-class securities. Price/capitalization fields are restricted to the required dates and their preceding trading dates, with returns omitted from this cap projection. There is no full-year, all-stock response read.
5. Invalid/nonpositive/nonadjacent lag classes and missing returns are counted. The retained-cap fraction is expressly conditional on observed adjacent positive-lag classes, not complete company coverage. Public deletion diagnostics use opaque labels rather than licensed company identifiers.
6. The previously omitted, unused historical-symbol metadata source is disclosed in the corrected source manifest; no response or roster recomputation was needed. The authoritative support file was corrected to valid JSON without changing its values.

Synthetic estimand, missing-gate, duplicate-key, insufficient-block, empty-support and static source-boundary fixtures pass after these finite corrections. The authoritative gate inputs remain the independently verified 4,904-row design, not the superseded duplicate product. The full eleven-file approval hash set is recorded in `PREOUTCOME_REVIEW.json`; any change requires a new finite review before response access. Final empirical verification was subsequently completed as recorded below.

## Final independent outcome-stage reproduction

After the signed opening, the referee read only the approved materialized SCC analysis panel and its metadata—not additional raw H2 outcomes. A separate full event-dummy weighted least-squares implementation independently formed cluster score vectors and the CR1 sandwich. It did not call the submitted estimation helper. The full parameter dimension was 17 (five slopes and twelve issuer-event intercepts), with nine calendar blocks and the specified eight reference degrees of freedom.

The panel hash is `63a8c106014baffa5e4bedf3dd594224d6eb9bc24a6cfc076fc83ccc6fce5b91`. Independent counts are 4,871 rows, 488 receivers, twelve issuer-events, nine blocks and 430 same-industry rows. All retained keys belong to the frozen design. The 33-row attrition reconciles to the builder receipt's 31 missing-event and two insufficient-control cases; no replacement observations were introduced. These reason counts are receipt-based; the referee independently verified the total and subset relation without reopening raw responses.

Post-response metadata checks independently retain rank five, rank five after every block deletion, variation in both groups in all nine blocks, maximum event-balanced block weight 1/6, at least two controls, and zero cross-block calendar sharing. The operational support rule therefore still passes. It remains an operational check, not a proof of independent shocks or power.

| Primary quantity, per fixed H1 SD of log coholding | Independent estimate | Independent SE | Bonferroni-family interval |
| --- | ---: | ---: | --- |
| Same-industry slope | -0.0011070618499289045 | 0.0011347424124686857 | [-0.004229332373290008, 0.002015208673432199] |
| Direct same-minus-other slope | -0.00031952694867404957 | 0.0012108941541497948 | [-0.003651330786152023, 0.003012276888803924] |

The independent `t(8)` critical value is `2.751523596063054` for the two-sided 97.5% marginal intervals. For the base and interaction slopes, the independently constructed covariance entries are `V(base,base)=6.576682517330167e-7`, `V(base,interaction)=-4.1814628081595534e-7` and `V(interaction,interaction)=1.466264652554147e-6`. The same-industry variance includes the twice-covariance term. Maximum discrepancy from the public primary estimates, standard errors and interval bounds is `1.26e-14`.

All sixteen deletion calculations were independently reproduced using the opaque public label mapping. Maximum coefficient discrepancy is `2.41e-14`.

| Sensitivity | Deletions | Positive same-industry slope | Positive direct difference |
| --- | ---: | ---: | ---: |
| Leave one calendar block | 9 | 0 | 3 |
| Leave one issuer | 7 | 0 | 2 |

The same-industry deletion slopes range from approximately -0.0021000 to -0.0003592 across blocks and -0.0018363 to -0.0004489 across issuers. This does not rescue the positive-support rule. Both primary intervals include zero and allow positive values, so neither `CONTINUE_MECHANISM_FEASIBILITY` nor the frozen nonpositive-upper-bound `STOP_SIGNAL_ROUTE` condition holds. The independently reproduced action is **`INCONCLUSIVE_STOP_SPENDING`**, not equivalence, zero spillovers, or rejection of every possible concentration mechanism.

## Final interpretation, execution and version closure

The final decision and README accurately separate failure to obtain positive replication support from proof of no effect. They acknowledge that the H2 event-balanced pooled interaction is not the identical H1 strict-subgroup estimand, and that repeated receivers/issuers and common shocks can violate the descriptive interval assumptions. Archiving this test and stopping automatic purchases/year expansion for this route is consistent with the frozen resource-allocation rule. There is no unresolved required arithmetic or implementation correction.

Execution receipt `7671847` records one submission, scheduler `failed=0`, `exit_status=0`, 24 seconds and 1.556 GB maximum virtual memory. Completion is supported by the scheduler-accounting receipt, not merely the earlier end-of-log message. The response and estimation receipts were written before final review and retain their historical pending-review labels; this document closes that review rather than rewriting their provenance.

All eleven pre-outcome bound files were independently rehashed on SCC after execution and remain unchanged. The two numerical result CSVs and response-build receipt match their SCC originals exactly. The public estimation receipt is a privacy-redacted derivative: its date-keyed block-rank entries are replaced by nine checks, minimum rank five and a labels-omitted flag. Its immutable SCC source hash is `68ca7eb7dd27550ec48e610e9992afffd6104c6b1205bb066da5d42ab60641c1`; its public hash appears below. The execution receipt explicitly records that transformation; it is not an unexplained hash discrepancy or an empirical rerun.

The full date-coded `AUTHORITATIVE_SUPPORT_GATE.json`, root `DESIGN_MATRIX_DIAGNOSTICS.json` and `metadata/DESIGN_MATRIX_DIAGNOSTICS.json` are not Git-publication artifacts. Their exact three task-generated local copies were removed before staging after SCC preservation/hash verification, as recorded in `LOCAL_PRIVACY_CLEANUP.md`; the public `SUPPORT_GATE_SUMMARY.json` supplies their safe aggregate conclusions. The reviewer confirmed that these files are absent locally, the date-coded diagnostic scan is clear, and `.gitignore` excludes their re-export and bulk private files. The approval continues to bind the full SCC inputs, not substituted public summaries. No claim is made that the temporarily local date-labeled diagnostics were never local. This closes the finite publication-privacy correction.

The pre-outcome ledger is preserved; `EXPOSURE_UPDATE.json` separately records the newly authorized access. Conditional share-class coverage of 1.0 must not be read as complete-company coverage: the receipt reports two invalid-lag/capitalization class-dates and explicitly restricts its coverage denominator.

Final verified artifact SHA-256 values:

| Artifact | SHA-256 |
| --- | --- |
| `PREOUTCOME_REVIEW.json` | `8ec64e0fea3e61af8b644884cabab5d5bf53346ae68676c8bd25979b2f0e618e` |
| `REPLICATION_RESULTS.csv` | `d44d5a98bcdab7339e2d321306eb544a9df532df2507b9ef9c0dea6a82ecb4f9` |
| `SENSITIVITY_RESULTS.csv` | `ed6bf2f41147b83402c72f43859a0ca156063de430243d2eefa1938d8360651a` |
| `RESPONSE_BUILD_RECEIPT.json` | `4c59c4f72937008193fab8a17b8f93df585b80d83ed5766a42234046a42caa68` |
| `ESTIMATION_RECEIPT.json` (public redaction) | `8d61634ed6c02d2be21c4b219a6f9ae073aec7af4e7f986e2990d3376c731955` |
| `EXECUTION_RECEIPT.json` | `8d5d4b32d674b95c43b19e38e5b1c829b7f8be53debe1ef20323ba06e27062a4` |
| `EXPOSURE_UPDATE.json` | `e6a23c771f200645d6d3a9233cc00b5091020b8d58242b2969df5aaeaa22d414` |
| `DECISION.md` | `a038c846cffc4873a6a16a60930286625d9319e3090625db3f38352df553b7c9` |
| `README.md` | `fd3a8d1364297766e0e2c08bbb4d72c942f25fdaf7d0b521c6bb07d7872afc7e` |

The parent integration manifest can hash this completed review separately. The reviewer did not commit, push, merge, purchase data or open unrelated outcomes.
