# News--ETF--actual-basket joint-support readiness

Status: **HOLD_DATA**. This is a reproducible re-accounting of existing permitted metadata summaries, not a fresh 32-row joint census, empirical sample, or data-absence conclusion.

Run `python3 sources/build_joint_support.py` from this directory. It reads seven named small receipts/manifests, writes `sources/STAGED_COUNTS.json`, and never connects to SCC or opens DBN/Parquet bodies. Input hashes are in that output and in `SOURCE_BINDINGS.json`.

## Staged results

| Stage | Count / denominator | What it establishes | What it does not establish |
| --- | ---: | --- | --- |
| Retained candidate release groups | 32 | Record inventory exists | A new approved population or authenticated economic news |
| First-public clocks certified | 0 / 32 | No retained candidate is currently eligible on the required clock evidence | That zero usable economic events exist in the underlying world |
| Conditional technical anchors | 2 / 32 | Two retained page-time diagnostics exist | First-public status or scientific eligibility |
| Unresolved clock records | 30 / 32 | Missing retained certification is explicitly counted | Zero timestamps at the source |
| Actual-fund snapshot files | 2 | QQQ Form N-PORT snapshots are retained actual-fund record/file evidence | Availability-matched holdings for any candidate event |
| Availability-matched actual baskets | UNKNOWN / 32 | Nothing; no authorized event--holdings join exists | Zero matched baskets or no fund data |
| SPY historical identity plus old-manifest symbol match | 32 / 32 | An old request manifest named SPY for each retained group | Prices, valid quotes, an all-window feed, ETF--basket comparability, or support for this design |
| QQQ historical identity plus old-manifest symbol match | 0 / 32 | No QQQ symbol match in that old request manifest | No QQQ data, no actual QQQ holdings, or no usable new-design coverage |
| Valid news--ETF--actual-basket quote support | UNKNOWN / 32 | Nothing; a custodian validity procedure has not been approved | Zero valid joint observations |

The 149 archived licensed ETF-holdings files are a 2022 aggregate inventory with status `IDENTITY_SUPPORT_ONLY_NOT_WEIGHTED_NETWORK`. They are neither parsed nor inherited as a 2023 basket population. A historical narrative reports a named-record count for the two N-PORT files, but it is **NOT_INDEPENDENTLY_RECOUNTED** here and is excluded from the staged counts. Index, PCF, prospectus mechanism, and secondary index sources remain proxies and are not relabeled actual baskets.

The old quote catalog contains four venue datasets and 5,544 old manifest rows. Its headers/intervals show only request/file accounting. Quote-body validity and two-sided status remain `NOT_ASSESSED`; no price, quote, size, return, or response value was read.

## One required first operation

No existing permission permits the new design's join. The only request prepared now is [the historical distribution-provenance adapter](sources/CUSTODIAN_OPERATION_REQUEST.json). It is restricted to the prior six-event, six-URL recheck, with input hash `267f9b679e9b61245268bf70a1026e1205576ffe611e4adf5cda998bd8e90dec`. `sources/first_public_adapter.py` is the custodian-side aggregate adapter: it rejects any ID outside that fixed set and emits only counts.

This request does **not** replay the prior page-timestamp parser: that could add no independent evidence and cannot turn page metadata into first-public provenance. It requests the exact currently unavailable publisher historical CMS/version or release-distribution view associated with those six exact URLs. If that view is absent, the correct result is `UNKNOWN`, not a claim that no earlier publication exists.

Existing permission evidence is exact: `../20260920_phase3/event_clock/RECEIPT.json` records the prior bounded public-metadata procedure, `CLOCK_SUMMARY.json` records its unresolved first-public status, and `build_event_clock.py` supplies the six fixed URLs. The request does not expand these materials. Rows stay in the custodian environment; the local receipt is aggregate only.

Availability-matched actual historical holdings, an ETF/issuer mapping for this new design, and a DBN quote-validity receipt remain dependencies, **not additional authorization requests in this execution**. Existing sources prove neither: the 2022 holdings inventory is identity-only; the N-PORT snapshots are not availability-matched; and the old DBN manifest has no valid-quote determination.

## Independent inventory-dependence recount and horizon boundary

The separate reviewer read the permitted public `CLOCK_EVIDENCE.csv` metadata and reproduced the following, saved with source hashes in `reviewer/INDEPENDENT_COUNTS.json`. These are inventory diagnostics, not an approved population, inference cluster count or effective sample size.

| Inventory quantity | Independently recounted |
| --- | ---: |
| Rows / unique old event IDs | 32 / 32 |
| Distinct issuers | 8 |
| Events per issuer, min / max | 4 / 4 |
| Distinct calendar event dates | 25 |
| Shared-date blocks / events in those blocks | 6 / 13 |
| Maximum events on one date | 3 |
| New-design cross-ETF event reuse | UNKNOWN: no authorized new-design join |

The reviewer also independently recounts two N-PORT source entries. SPY 32/32 and QQQ 0/32 remain **propagated safe-summary accounting**, not independently reconstructed security-window rows. No exposure, news-strength or liquidity common-support assessment was made.

For **each** proposed continuous-clock horizon 1, 5, 15, 30 and 60 minutes, valid stock-constituent coverage, ETF coverage, and their common mask are UNKNOWN. RTH/pre-open/after-close and RTH-60 counts for the new design are also UNKNOWN, not zero. No new first-public clock, actual basket or valid quote-state join exists to certify these dimensions. V1 horizons and session choices remain proposed, not silently frozen from these unknowns.
