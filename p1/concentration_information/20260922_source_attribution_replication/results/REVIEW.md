# 独立有限复核：PASS_WITH_LIMITATIONS

日期：2026-09-22。复核对象为 SCC 最终 `summary_final`、四个 2024 cell、四个 `frozen_2023` 对象、2024 特征/原始文件 receipts，以及本地最终报告。未重新启动全项目审计，未购买数据，未提交或推送。

## 结论

未发现需要修复的实质错误。六模型列归属、时间控制、跨期冻结、fixed-A2-lambda、四项预定比较、日期 bootstrap、24 次 leave-one-date-out、数据清单和抽样字段血缘均通过。主决定 `LIMITED_TO_INFORMATION_SET` 由现有证据支持。

该判断仍限于条件预测信息集：它不识别结构性信息份额、因果领先或 ETF 是否参与价格发现，也没有事前经济等价界限。

## 1. 六模型、时间控制和跨期冻结

- 逐一读取四个冻结对象中 23 股的有序 `raw_columns`。列块与执行规格完全一致：A0=B、A1=B+Q、A2=B+C、A3=B+C+Q、A4=B+Q+P、A5=B+C+Q+P；未发现未知列或 own/fixed 列表差异。
- 原始列数分别为 A0 37、A1 45、A2 64、A3 72、A4 54、A5 81。这对应 B=37（目标股报价 8、rest 报价 8、rest 覆盖 8、ES 报价 8、时间控制 5）、Q=8、C=27、P=9。
- 时间控制只有 `minute_1` 至 `minute_5`；整数源列 `minute_bin_5m` 没有作为第六个控制进入模型。
- 四个冻结对象的训练、验证、探索性测试日期全部为 2023 年；对象状态均为 `FROZEN_2023_BEFORE_2024_SCORE`。冻结模型代码 hash 为 `5b22680a...9712a`，与四个 2024 cell receipt 和本地评分代码一致。
- 评分路径只用冻结对象中的列顺序、训练中位数、均值、尺度、lambda、截距和系数；没有从 2024 重估插补、缩放、lambda 或系数。冻结对象完成时间早于四个 2024 score receipt。
- fixed-A2-lambda 检查覆盖 4 格 × 23 股 × 6 模型：每个 fixed 模型都使用同股同格 A2 在 2023 验证期选定的 lambda，mismatch=0。

## 2. 独立复算

从四个 `model_parts_2024/*/DAILY_LOSS_CELL.csv` 独立宽表化并重算，而不是读取发布点估计。日级 SSE 回加至 cell 汇总的最大误差为 `5.82e-11`，样本数完全一致。

下表为 23 股等权、四格等权结果；方括号为 2,000 次同步完整日期 bootstrap 的 95% 分位区间。

| 拟合规格 | A0→A1 | A2→A3 | A3→A5 | A2→A5 |
|---|---:|---:|---:|---:|
| OWN_LAMBDA | 0.000205 [0.000071, 0.000315] | 0.000091 [−0.000012, 0.000192] | −0.000247 [−0.000325, −0.000186] | −0.000156 [−0.000310, −0.000020] |
| FIXED_A2_LAMBDA | −0.000052 [−0.000183, 0.000058] | −0.000064 [−0.000197, 0.000045] | −0.000256 [−0.000329, −0.000206] | −0.000321 [−0.000485, −0.000194] |

独立点估计、四格估计、report-weight 辅助估计及 bootstrap 区间与 `PAIRED_CONTRASTS.csv` 的最大绝对差为 `1.00e-16`。日期抽样矩阵对全部股票、模型、场所和网格共同使用。

独立复算 24 次 LOO（每项比较、每套拟合各 24 次，共 192 个值）与 `DATE_SENSITIVITY.csv` 最大差为 `9.97e-17`。主 A2→A5 的 LOO 范围为：OWN_LAMBDA `[-0.000188, -0.000130]`，FIXED_A2_LAMBDA `[-0.000353, -0.000293]`，均未翻号。

## 3. 2024 中心与字段血缘抽查

抽查 24 个中心：2024-01-08 与 2024-08-07 各 12 个，覆盖 ARCX/XNAS、0/500ms，并在每格取 `second_index` 60、900、1740。仅重解这两日的 6 个原始文件，不重解全量 DBN。

- 24/24 中心的目标股、SPY 与 ES 的 `date/grid_shift_ms/second_index/t_ns` 一致；24/24 目标有限、SPY 报价源字段有限、`es_valid=1` 且 ES 八个报价字段有限。
- 对 AAPL 和 SPY 的报价、分离成交、诊断字段和 `y_1s_bp` 从原始 DBN 重建；对 ES 八字段从原始文件重建。与持久化 parquet 的最大绝对差分别为 `8.88e-12` 和 `1.78e-11`，属于序列化/浮点舍入量级。
- 手工按中心时点与 `t+1s` 的 midpoint 复算目标，差异同为约 `1e-11` 舍入量级；未发现预测字段使用中心之后事件。实现以 `ts_event` 排序并在中心或之前取状态，目标明确使用未来 1 秒。
- 6 个抽查原始文件的实际 SHA-256 均与下载 receipt 一致。

## 4. 清单、费用与凭据

- 股票特征包含固定 23 股加 SPY，共 24 个 symbol；BF 未进入分析。外部样本含 24 个固定日期、两场所和两网格。
- 下载 receipt 有 72 个唯一请求及 72 个唯一非空原始文件：XNAS 24、ARCX 24、GLBX ES 24；每日期恰有 3 个源文件。SCC 目录实际也有 72 个 `.dbn.zst`。
- 全年 72 窗口的 API 报价为 USD 7.057148829108；receipt 明确这是报价而非账单扣款证明。36 个文件标为复用，36 个标为新增下载。
- 扫描 `QUOTE_RECEIPT.json`、`DOWNLOAD_RECEIPT.json`、`REQUEST_MANIFEST.json`、`DATA_ENGINEERING_RECEIPT.json` 等 receipt 未发现密钥、token、authorization header 或密码值。出现的 `credential_value_logged: false` 只是安全状态字段，不是凭据。

## 5. 决定复核

证据支持 `LIMITED_TO_INFORMATION_SET`：主调参规格下弱基准 A0→A1 为小幅正值且区间在零上方，但控制 C 后 A2→A3 区间跨零，SPY 成交 A3→A5 为负，预定主 A2→A5 为负且区间在零下方；24 次 LOO 不依赖单日。固定 A2 lambda 下 A2→A5 更负，且四格方向一致。与此同时，report-weight 辅助结果与场所异质性提示结果依赖权重与模型程序，因此不能升级为 ETF 无信息或现金市场因果吸收 ETF 信息。

复核状态为 `PASS_WITH_LIMITATIONS`：数值和工程边界通过；科学解释必须继续限制在已测信息集的一秒样本外预测差异。
