# Targeted acquisition authority

The user subsequently instructed: “不用查报价，直接下就行了。” This removes per-order quotation/approval interruptions; it does not request duplicate downloads of existing files or waive scientific coverage checks on bid/ask observations.

Acquire a named missing dataset/window directly when it resolves a specified measurement gap, using configured authentication and verified remaining credits. Record vendor charges/order receipts and avoid duplicate requests. Do not treat old chat balances as a verified current balance or disclose credentials. Existing local environment boolean check returned DATABENTO_API_KEY_PRESENT=False; no historical key or credential store was accessed.

The current 29-file diagnostic reads already acquired SCC files and makes no market-data API calls. No purchase is necessary for that diagnostic. No additional order has been placed in this stage.
