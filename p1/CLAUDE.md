# p1/CLAUDE.md — seat scope

This directory is owned by exactly one Claude seat (see ../ops/accounts.yaml).
Do not edit files outside p1/ and shared/ (shared/ read-only unless seat D).
Paste the project's CONTEXT PACK / C0 header at the top of every task prompt.
Task prompts (verbatim) live in ../docs/ — copy the block for your task id into
your brief. Frozen output schemas: ../ops/contracts/.
