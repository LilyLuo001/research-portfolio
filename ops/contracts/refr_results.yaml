# REFR-R6 output — dual-implementation main estimates (manual §R6: Py + R,
# per-spec point estimates compared at 1e-6; prereg_guard hard check at startup)
producer: REFR-R6-py
format: directory
required_files: [main_py.json, main_r.json]
