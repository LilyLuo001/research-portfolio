# L2 audit — 2026-07-10 (owner-ordered, pre-L1-automation gate)

_Scope: every L2 deliverable completed 2026-07-09/10, audited on two axes —
technical (runs, reproduces, contracts pass) and knowledge (numbers trace to
sources, claims internally consistent). Auditor: Anthropic L2 session,
owner-delegated. Verdict up front: **no gate decision is invalidated; one
real knowledge error found (K-1); four technical risks found, two fixed in
this commit, two flagged before arming.**_

## Verdicts by deliverable

| deliverable | technical | knowledge |
|---|---|---|
| P1-T2a-power (sim + memo + CSV) | ✅ sim **reproduces byte-identically** (seed 20260709); every memo number recomputed by hand — all 9 MDE cells, kill-switch 33/8, verification power 0.787 vs analytic 0.82 (within MC noise, 150 reps) — all check; contract PASS; lineage hashes present | ⚠ K-1, K-2 below; priors honestly labeled; θ NEED_HUMAN already flagged in memo §4 |
| DAX-W0-infra (guard + CI) | ✅ guard **live-tested fail-closed** (refuses with no tag, fails closed if git unavailable); 3 defense layers coherent; lineage CI; NDA grep excludes prose correctly; tests in the green 69 | ✅ no factual content |
| E2-T1-facts channel A + union | ✅ sentinel 3/3 claim **verified against the spec's expects** (Morpho / 2025-08 / Polygon); structure + per-row URLs check | ✅ arbitration recorded (§仲裁); FalconX redemption correctly UNKNOWN-pending |
| P1-T0-crash channel A sweep | ✅ fenced (S1 = FEDS Note date ✓) | ✅ internally consistent: FEDS-Note outcome variables (volatility/liquidity) genuinely differ from P1's (earnings-information incorporation); honest UNKNOWNs with search paths; the one adjacency risk (adverse-selection costs) is explicitly carried, not buried |
| DAX-W0.5-legwork channel A | ✅ fenced 3/3; honest confidence labels | ⚠ K-3 below |
| runner.py py3.6 fix | ✅ correct (same strptime parse as lease.py, both call sites) | — |
| E2-T9b re-route (tonight's dispatch) | ✅ gemini_free, 4 chunks + 2 sentinels, ¥0; S2 fence risk pre-documented | ✅ sentinels owner-verified facts |
| EDGAR harvester (runs tonight) | ⚠ T-1, T-2 **fixed in this commit**; T-3 flagged | ⚠ K-4 below |
| E2-T2-dune spec (armed-but-parked) | ⚠ T-4 flagged in the spec header | prompts match manual T2 structure |

## Technical findings

- **T-1 (fixed): silent truncation.** `MAX_PAGES=20` caps each query at 200
  hits with no signal. A conversion-language query exceeding that would
  silently shrink the event universe — the worst kind of miss for an event
  study. Now prints a loud WARNING with the true hit count; check
  `harvest.log` for it after tonight's run and raise MAX_PAGES if it fires.
- **T-2 (fixed): partial-download poisoning.** A crash mid-write left a
  truncated `.htm` that the skip-if-exists resume would treat as complete.
  Now write-to-`.part` + rename (atomic on POSIX).
- **T-3 (watch item): the FTS endpoint/response shape is unverifiable from
  this session** — SEC hosts 403 through the proxy. Failure mode is loud
  (raise_for_status → RuntimeError in harvest.log), not silent. Treat
  tonight's box run as the smoke test; if it errors, the endpoint/params
  need a browser look at efts.sec.gov before re-running.
- **T-4 (blocker for arming E2-T2-dune, note added to the spec): sentinel/
  modality mismatch.** S2/S3 ask post-2025 world knowledge of a deepseek
  worker with `web_search: false`. The kimi history shows exactly this
  UNKNOWN-on-armed-sentinel failure mode; here it would burn strikes on a
  task (SQL generation) that needs no such knowledge. Swap S2/S3 for stable
  in-domain facts before deleting `manual: true`.
- (observation, not a defect) CI gates PRs, but the lanes push straight to
  main, so enforcement is post-merge. Fine while every actor also runs
  selfcheck locally; revisit if a red main ever ships.

## Knowledge findings

- **K-1 (the one real error): the AUM cross-check contradicts itself.**
  `p1/events_from_note.csv` and the memo both state the four-fund sum is
  "~$28.65bn" vs the $28.8bn ETF-Strategy headline — but the four per-fund
  figures actually in the CSV (5.5 + 14.4 + 4.2 + 6.6) sum to **$30.7bn**.
  Either one per-fund figure is wrong or the stated sum is; sources
  (seekingalpha article body) 403'd, so it is not resolvable from here.
  **Gate impact: none** — dose scales linearly in AUM, a ±7% error moves
  MDEs far less than the ×1.5 conservatism, and all three bands pass with
  3–6× margin; `gate P1-GATE-t2a pass` stands. But the CSV feeds T5 later →
  **owner browser item: verify the four per-fund AUMs, then fix CSV + memo
  together and re-run the sim (seed fixed) — do NOT edit the CSV alone, its
  hash is pinned in power_memo.md.lineage.json.**
- **K-2 (wording): memo §2 structural finding 3** says "γ=1.0, θ=50bps 时
  N_T→0" — its own table says N_T=357 for that cell. Directionally right
  (collapse from 1039), literally wrong. Fix at next memo touch; does not
  affect the gate.
- **K-3: the 2026-07-23 gpt-3.5-turbo shutdown deadline** rests on a
  community post + aggregator (official deprecations page 403'd; confidence
  中, self-flagged). The feasibility block's urgency is real either way
  (gpt-4/4o/o1 already retired/retiring), but browser-verify the exact date
  before writing it into `dax/memo/feasibility_note.md`.
- **K-4: harvest phrase coverage.** The seven FTS queries skew to
  "conversion" phrasing; SEC filings often use "reorganization … into an
  exchange-traded fund". Misses at harvest are invisible to the dual-channel
  extraction downstream. After tonight's run, review per-query hit counts in
  harvest.log and add reorganization-family phrases + re-run (idempotent)
  before spec'ing P1-T1-events.

## What this clears

L1 automation may proceed as staged: tonight's dispatches (E2-T9b gemini,
harvester smoke-by-running) are safe; the four P1-T1/T13 specs wait for the
manifest + K-4 review; E2-T2-dune arming waits for T-4 re-fence + owner
ARMING inputs. The two owner browser items (K-1 AUMs, K-3 date) are queued
in L2-PIPELINE.md §3.
