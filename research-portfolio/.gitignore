__pycache__/
*.pyc

# box-only secrets + runtime logs (never leave the always-on box)
ops/box/.env
ops/box/cron.log
ops/runner/spend_log.jsonl
ops/runner/state.json      # runner completion/gate state — box-owned runtime
